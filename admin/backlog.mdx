---
title: "Security & Infrastructure Backlog"
description: "Lower-priority vulnerabilities and hardening opportunities identified during audits. Sorted by risk level."
visibility: internal
---

## Overview

This document captures findings from Phase 2 Deep Audits that were below the Top 3 threshold but still represent meaningful risk. Each entry includes: Risk Level, The Vulnerability, Proposed Fix, and ROI.

**Last updated:** 2026-04-20

---

## Issue 4: Turnstile Bypass is a Side-Channel for Enumeration

**Risk Level:** Medium-High

**The Vulnerability:** The `shouldSkipTurnstile` function in `supabase/functions/_shared/turnstile.ts` allows enumeration of admin email addresses. If an attacker sends repeated requests with guessed emails and `turnstileToken: 'localhost-bypass'`, a successful skip reveals a valid admin account. The email pattern check (`kenlyle+` prefix) leaks the naming convention.

**Proposed Fix:**
1. Remove the `isAdminEmail` check from `shouldSkipTurnstile` entirely.
2. Instead, validate admin status against the `profiles` table (checking `is_admin = true`) after auth resolution, not before. The localhost bypass should require a signed JWT with an `admin` claim, not a guessable token string.

**ROI:** Prevents brute-force enumeration of admin accounts via the generation API.

---

## Issue 5: `usage_daily` / `usage_monthly` Race Condition on Simultaneous Generations

**Risk Level:** Medium

**The Vulnerability:** The `deduct_credits_atomic` RPC uses `SELECT ... FOR UPDATE` to lock the `profiles` row during credit deduction. This is correct for a single generation. However, if a user opens two browser tabs and clicks Generate simultaneously, both requests race. The first lock holder deducts credits and returns. The second lock holder then reads the already-decremented value. The atomic function correctly prevents overdraft, but the UX shows two "Generating" states and only one result ever completes. The user pays twice and may think the second generated idea was lost.

**Proposed Fix:**
1. Add a `pending_generations` integer column to `profiles`.
2. Increment before starting generation, decrement on completion (via webhook or timeout).
3. Check `pending_generations > 0` in the frontend before allowing a second Generate click.

**ROI:** Prevents double-charge confusion from users mashing the Generate button.

---

## Issue 6: Billing Events Table Has No Retention Policy

**Risk Level:** Low-Medium

**The Vulnerability:** `billing_events` accumulates indefinitely. Each WooCommerce webhook creates a row. At scale (1,000 customers renewing monthly), this table grows by 12,000 rows per year. The `raw_payload` JSONB column stores full webhook payloads including billing addresses — PII that should not be retained longer than necessary.

**Proposed Fix:**
1. Add a pg_cron job that deletes `billing_events` older than 90 days where `processing_status = 'success'`.
2. For `failed` events, retain for 1 year to support debugging.
3. Anonymize `raw_payload` by stripping billing addresses after 30 days.

**ROI:** Reduces database storage costs and GDPR compliance surface area.

---

## Issue 7: Gallery `handleUpload` Has No Storage Cleanup on Partial Failures

**Risk Level:** Low-Medium

**The Vulnerability:** In `app/(app)/gallery/page.tsx`, the upload handler first writes to Supabase Storage, then inserts into `user_images`. If the DB insert fails (e.g., RLS violation, connection timeout), the storage object remains orphaned. There is no cleanup call in the catch block.

**Proposed Fix:** In the `handleUpload` catch block, call `supabase.storage.from('user-gallery-images').remove([filePath])` to delete the orphaned object before surfacing the error.

**ROI:** Prevents storage bucket bloat from failed uploads.

---

## Issue 8: `outscraper-worker` Webhook Secret is Checked After Body Parse

**Risk Level:** Low

**The Vulnerability:** In `supabase/functions/outscraper-worker/index.ts`, the webhook secret is validated after `await req.json()`. A malicious actor could send a large payload to exhaust Deno memory before the secret check rejects the request. This is a DoS vector, albeit low-impact for Supabase Edge Functions (they have memory limits).

**Proposed Fix:** Move the `x-webhook-secret` validation to the top of the handler, before any body parsing.

**ROI:** Prevents memory-exhaustion DoS on the outscraper-worker edge function.

---

## Issue 9: No Rate Limiting on `/api/social/*/post` Routes

**Risk Level:** Low

**The Vulnerability:** The social publish API routes (`/api/social/facebook/post`, `/api/social/linkedin/post`) have no rate limiting beyond the Supabase auth check. A compromised account could spam-publish to social platforms, hitting API rate limits and potentially getting the OAuth app blacklisted.

**Proposed Fix:**
1. Add a `last_publish_at` timestamp to `social_connections`.
2. In each post route, check if `last_publish_at` is within the last 10 seconds. If so, return 429.
3. Update `last_publish_at` on every successful publish.

**ROI:** Protects social platform API health and prevents account suspension.

---

## Issue 10: `generate-text` Edge Function Does Not Validate `count` Parameter

**Risk Level:** Low

**The Vulnerability:** The `count` parameter in `generate-text/index.ts` defaults to 1 and is never validated against a maximum. A malicious authenticated user could send `count: 100` and generate 100 ideas in a single request, burning credits and overwhelming the OpenAI API.

**Proposed Fix:** Add `count = Math.min(Math.max(1, count), 5)` at the top of the handler.

**ROI:** Prevents credit arbitrage and API abuse.

---

## Backlog Protocol

When performing future audits:
1. Present Top 3 for immediate approval.
2. Append Issues 4+ to this file with Risk Level and ROI.
3. If an Issue is elevated to Top 3 status, move it to the top of this file and mark `status: 'in_progress'`.
4. When an Issue is fixed, move it to a "Closed" section with fix date and files changed.

**Closed Issues:**

| # | Issue | Fixed | Files changed |
|---|---|---|---|---|
| 1 | FAL Webhook → Realtime Death Spiral | 2026-04-20 | `supabase/functions/fal-webhook/index.ts`, `hooks/useImageGeneration.ts` |
| 2 | Trial pg_cron Silent Absence | 2026-04-20 | `hooks/useCurrentUser.ts` |
| 3 | Playwright Zero Network Retry | 2026-04-20 | `scripts/_shared/resilient.cjs`, `app-02-usage`, `app-03-vault`, `social-01-fb-publish` |
| 4 | Credit Double-Deduction on Retry | 2026-04-20 | `supabase/functions/execute-image/index.ts`, `lib/actions/imageActions.ts`, `app/(app)/generate/page.tsx` |
| 5 | WooCommerce Trial Injection Fire-and-Forget | 2026-04-20 | `supabase/functions/wpsubscription-webhook/index.ts`, migration for `trial_injected_at` |
| 6 | Gallery Optimistic UI Without Error Recovery | 2026-04-20 | `app/(app)/gallery/page.tsx`, `components/gallery/ImageDetailsDialog.tsx` |
| 7 | billing_events Permanent Failures Invisible | 2026-04-20 | `supabase/migrations/`, `supabase/functions/billing-recon/index.ts`, `wpsubscription-webhook/index.ts` |
| 8 | Turnstile Bypass is a Side-Channel for Enumeration | 2026-04-21 | `supabase/functions/_shared/turnstile.ts` |
| 9 | `usage_daily` / `usage_monthly` Race Condition on Simultaneous Generations | 2026-04-21 | `supabase/functions/generate-text/index.ts` |
| 10 | Gallery `handleUpload` Has No Storage Cleanup on Partial Failures | 2026-04-21 | `app/(app)/gallery/page.tsx` |
| 11 | `outscraper-worker` Webhook Secret is Checked After Body Parse | 2026-04-21 | `supabase/functions/outscraper-worker/index.ts` |
| 12 | No Rate Limiting on `/api/social/*/post` Routes | 2026-04-21 | `app/api/social/facebook/post/route.ts`, `app/api/social/linkedin/post/route.ts`, `app/api/social/instagram/post/route.ts`, `supabase/migrations/20260421061400_social_rate_limit.sql` |
| 13 | `generate-text` Edge Function Does Not Validate `count` Parameter | 2026-04-21 | `supabase/functions/generate-text/index.ts` |
| 14 | Billing Events Table Has No Retention Policy | 2026-04-21 | `supabase/migrations/20260421061500_billing_retention.sql` |

---

## Phase 3 Global Verify Findings (2026-04-21)

Discovered during holistic conflict detection between Credit Idempotency, WPSubscription Webhook, and Loops.so Recon Agent.

### Critical Path Conflicts (Fix Immediately)

| # | Issue | File | Risk | ROI |
|---|---|---|---|---|
| 15 | execute-image idempotency check is NOT atomic — two requests can both pass the guard and deduct credits | `supabase/functions/execute-image/index.ts:44-80` | Double credit deduction on rapid clicks | Prevents "you charged me twice" tickets that `alreadyProcessing` flag was supposed to eliminate |
| 16 | billing-recon can send duplicate Loops emails if DB update fails after Loops success | `supabase/functions/billing-recon/index.ts:107-127` | Email storm during network instability | Prevents inbox flooding and Loops API overage charges |
| 17 | Gallery upload cleanup can fail silently — `.catch(() => {})` swallows storage removal errors | `app/(app)/gallery/page.tsx:369` | Orphaned files accumulate in storage bucket | Prevents storage bloat from failed uploads |

### Deep State Audit Findings (Schedule Next Sprint)

| # | Issue | File | Risk | ROI |
|---|---|---|---|---|
| 18 | `imageGeneratingIds` cleared on timeout allows duplicate generation before webhook arrives | `hooks/useImageGeneration.ts:54-80` | User clicks "Generate Again" at 102s, triggering duplicate | Prevents double generation charges after timeout |
| 19 | `wp-subscription-webhook` does not validate `subscription_id` type before string operations | `supabase/functions/wpsubscription-webhook/index.ts:293-300` | `null` subscription_id causes injection failure | Defense in depth against WooCommerce payload variations |
| 20 | `billing-recon` Loops alert has no retry on transient failure | `supabase/functions/billing-recon/index.ts:22-48` | Alerts lost during Loops outages | Ensures billing failures are never silently missed |
| 21 | Social post routes have 120 lines of duplicated `checkRateLimit` / `markPublished` code | `app/api/social/*/post/route.ts` | Maintenance burden, inconsistent limits | Reduces tech debt and risk of divergent rate limits |
| 22 | `generate-text` 60-second guard is per-user, not per-profile | `supabase/functions/generate-text/index.ts:93-103` | Cannot generate for different businesses within 60s | Removes UX friction for multi-business users |
| 23 | `fal-webhook` JWT has no `iat`/`exp` check | `supabase/functions/fal-webhook/index.ts` | Replay attack on old JWTs | Defense in depth for webhook security |
| 24 | `outscraper-worker` has no max payload size guard | `supabase/functions/outscraper-worker/index.ts:469` | Memory exhaustion from oversized payloads | Prevents DoS from malformed webhook requests |

### Full Backlog (Issues 25–34)

| # | Issue | File | Risk | ROI |
|---|---|---|---|---|
| 25 | Social connections GET returns expired tokens as "connected" | `app/api/social/connections/route.ts:19` | Users see "connected" but posts fail | Prevents "my posts stopped working" confusion |
| 26 | `secureCompare` nonce generation is dead code | `supabase/functions/wpsubscription-webhook/index.ts:222` | Unused complexity increases audit surface | Cleaner codebase, easier security review |
| 27 | Gallery auto-tag failures silently swallowed | `app/(app)/gallery/page.tsx:361` | Images uploaded without tags | Prevents "my images have no tags" tickets |
| 28 | `generate-text` batch loop has no upper bound defense | `supabase/functions/generate-text/index.ts:267` | Unbounded loop if count cap is removed | Defense in depth |
| 29 | Channel subscription errors show generic toast regardless of cause | `hooks/useImageGeneration.ts:210` | Users cannot distinguish timeout from auth error | Better error messages reduce support tickets |
| 30 | WordPress sync failures are invisible | `lib/actions/galleryActions.ts:158` | Images don't appear on customer's website | Prevents "my images aren't on my site" tickets |
| 31 | Parallel image generation trusts `imageCount` from execution plan | `supabase/functions/execute-image/index.ts:233` | Tampered plan could create undefined promises | Input validation gap |
| 32 | Facebook post success doesn't return `pageId` used | `app/api/social/facebook/post/route.ts:107` | Cannot verify which page was posted to | Debugging friction for multi-page users |
| 33 | `recoveryLink` in billing-recon hardcodes Supabase URL | `supabase/functions/billing-recon/index.ts:105` | Link wrong if project moves regions | Operational friction during incidents |
| 34 | LinkedIn `postId` extraction not validated | `app/api/social/linkedin/post/route.ts:161` | Header name change causes silent null | Silent data loss in post history |
