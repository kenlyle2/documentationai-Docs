---
title: "Automated Testing"
description: "Full test suite documentation: all scripts, shared utilities, accounts, what they test, and chain reasoning."
visibility: internal
verified_against_commit: 6c31668f
---

<Callout kind="info">
  **Refreshed 2026-08-07** against current `postglider-auto` `main` (979 commits ahead of the
  previous `verified_against_commit: 0c7bb31`). This pass cross-checked every script this doc
  names against what's actually on disk today, added scripts that didn't exist yet at the last
  verification, and corrected sections that had drifted from the real file content. It also adds
  the `tests/e2e-integrated/` suite, which post-dates the last verification entirely and is now
  the most current, most rigorous test in the repo. Some deep-dive sections for scripts added
  since 0c7bb31 (`app-07`, `app-10/11`, `app-13/14/16`, `app-18/19/20`, `onboard-05` through
  `onboard-13`, `social-04`, `billing-02`) are intentionally summarized in the tables rather than
  given full narrative write-ups, to keep this refresh scoped — flag to Ken if full deep-dives on
  those are wanted next.

  **Same-day follow-up:** `chaos-onboarding-walkthrough.cjs` and `chaos-fuzzer.cjs` added (see
  Root-Level Admin & Verification Scripts below) — the former runs **automatically every day**,
  the first scheduled, unattended regression check in this repo. Every future test script must be
  added to this inventory as part of being considered done — see `postglider-auto/CLAUDE.md`'s
  Definition of Done.

  **2026-08-08:** the daily schedule above was corrected from a GitHub Actions workflow (deleted)
  to `pg-admin`'s real, already-existing Cloud Run Job + Cloud Scheduler pattern — see the
  `chaos-onboarding-walkthrough.cjs` table row below and `pg-admin/CLAUDE.md`'s Tenants list.
  Also, `app-04-mobile`'s real pixel-diff visual regression — flagged as orphaned in the
  refresh above — has been restored, plus a new baseline-independent clipped-content check added
  on top. See its own section below for the full detail; this is no longer a known gap.
</Callout>

## Overview

All test scripts live in `scripts/` and use `playwright-core` from project devDependencies — no global Playwright install needed. Every script requires the dev server running on `localhost:3000`.

```bash
# Start the dev server before running any script
rundev
```

### Running the Suite

```bash
node scripts/run-all.cjs              # full suite — all scripts in order
node scripts/run-all.cjs imagegen     # only the image pipeline unit tests (no browser, no server)
node scripts/run-all.cjs onboard      # only the four /start onboarding flows
node scripts/run-all.cjs app          # only the authenticated-user tests
node scripts/run-all.cjs social       # only the social-publishing tests
```

`run-all.cjs` runs scripts sequentially, streams each script's output live, and prints a grouped summary table with pass/fail and elapsed time at the end. Exit code is 1 if any script fails.

---

## Unit Tests (`vitest`)

Fast, pure-function tests — no browser, no dev server, no real API calls, no billed usage. Live under `lib/**/__tests__/*.test.ts`, run with `npx vitest run [path]` (or bare `npx vitest run` for the whole suite). These exist specifically to catch budget/routing-math regressions *before* they cost real money on a live batch run, not after.

| Test file | What it guards | Run |
|---|---|---|
| `lib/autonomous/__tests__/batchFillBudget.test.ts` | `computeOvershootCap`/`computeMaxBatchTotal`/`computeAutonomousFill` (the batch-size-to-post-count budget math) — the exact 2026-08-14 `MAX_GUARANTEED_PER_TYPE` regression (14 posts from a `batchSize:1` request) reproduced as a permanent case. | `npx vitest run lib/autonomous/__tests__/batchFillBudget.test.ts` |
| `lib/autonomous/__tests__/marketingAgentBudgetIntention.test.ts` | Built 2026-08-15 (decisions.md D-2026-08-15i) after Ken asked "we clicked Batch Size 7, got 5 posts — was that purposeful?" This test confirms the budget *arithmetic* is correct (`batchSize:7` always threads a 9-total-post intention into Marketing Agent's real BUDGET line) — it does NOT confirm the delivered batch actually reaches 9; that turned out to be a real, separate gap (D-2026-08-16c/d — Marketing Agent could undershoot its own budget with no backfill, since fixed by hardening the prompt). Calls the real production `assembleBatchCompletion()`/`buildMarketingAgentPrompt()`, not a reimplementation. | `npx vitest run lib/autonomous/__tests__/marketingAgentBudgetIntention.test.ts` |
| `lib/autonomous/__tests__/backlogMatcher.test.ts` | Built 2026-08-16 (decisions.md D-2026-08-16e) for the Backlog-reuse mechanism — "look there first once you know what you are looking for" (Ken). Tests `scoreBacklogMatch()` (the pure keyword-overlap scorer, reusing `vaultMatcher.ts`'s primitives) directly with plain candidate data, no Supabase mock needed. Confirms a concept matches the right banked idea, no-match returns null below the content-match threshold, and the highest-scoring candidate wins among partial matches. | `npx vitest run lib/autonomous/__tests__/backlogMatcher.test.ts` |

---

## Scripts

| Script | Account | What it tests |
|---|---|---|
| `onboard-01-staytrue` | `test+staytruetattoopa@gmail.com` (fake) | `/start`/`/onboarding` flow — StayTrue Tattoo PA: enrichment, GMB check, business form, real photo upload, generate, claim, `/generate` smoke check. Prod-capable by default (`resolveBase()`), `--dev` for localhost. |
| `onboard-02-colono` | `test+colonoconstruccion@gmail.com` (fake) | Same flow + direct enrichment API call first; audits every field by name. Prod-capable by default. |
| `onboard-03-roccos` | `test+roccospizza@gmail.com` (fake) | Same flow as colono — Rocco's Pizza Collegeville PA (restaurant type). Prod-capable by default. |
| `onboard-04-trial` | `test+trial-<timestamp>@postglider.com` (fake) | 7-day reverse trial lifecycle via API/DB only (no browser): claim → `tier=pro`+`trial_ends_at`, then backdates `trial_ends_at` and runs the real `pg_cron` downgrade SQL, asserts flip back to `tier=free`. **Hardcoded to `localhost:3000`** — local-only despite the naming convention. |
| `onboard-05-manual-l22` | synthetic "L22 Test Tattoo Co." | API/DB test of the manual (no-website) onboarding body path — asserts `business_profiles.onboarding_source='manual'` and non-empty classification codes. Local-only (`localhost:3000`). |
| `onboard-06-skipforward-l22` | synthetic "Skipforward Test Co." | Regression for the `{existingProfileId}` skip-forward fix — posting just an existing profile ID completes via that row instead of duplicating it. Local-only. |
| `onboard-07-rejected-posts-nudger-l23` | synthetic "L23 Nudger Test Co" | Rejected Posts Nudger prefetch route: 3-strike `butler_skip_count` detection, LLM summary fires once then doesn't re-fire on a stale call. Notes downstream quota/prompt-injection wiring is typecheck-only, not exercised live. Local-only. |
| `onboard-08-service-intelligence-l12` | synthetic "L12 Test Tattoo Co." | Confirms onboarding-seeded services get `business_services.intelligence` auto-populated by the fire-and-forget background call (polls up to 30s). Local-only. |
| `onboard-09-competitor-bridge-l17` | synthetic profile + `TEST_USER_ID` | Confirms manually-curated `public.competitors` rows bridge into `autonomous.competitor_accounts` via the real discovery RPC. Self-documents a known teardown gap (FK-blocked cleanup). Local-only. |
| `onboard-10-seasonal-schedule-l18` | 2 synthetic profiles | Confirms the live `/api/packs/create-from-ideas` route (not just the pure scheduling function) actually prioritizes Promote for a service in its seasonal peak month. Local-only. |
| `onboard-11-virality-signals-l19` | synthetic "L19 Virality Test Co." | Direct RPC round-trip test of Virality Agent signal persistence (`autonomous_insert_virality_signal`/`autonomous_get_recent_virality_signals`), including a real time-window filter check. No dev server needed at all. |
| `onboard-12-equipment-context-refresh-l15` | synthetic "L15 Equipment Test Co." | Confirms `/api/generate-equipment-context` re-derives equipment from the *current* services list, not a frozen snapshot. Local-only. |
| `onboard-13-content-calendar-seeder-l21` | synthetic "L21 Seeder Test Co." | Calls the real `selectFirstWeekAngles()` (via a spawned `tsx` child process, not HTTP) and asserts the 2 Promote / 3 Provide / 2 Personalize 3Ps quota is met from a 9-angle candidate pool. |
| `app-01-ux` | `kenlyle+2@gmail.com` (real, landscaper) | Auth + navigation regression: login branding, sidebar links, gallery search, mobile overflow, sign-out. Flips `is_pilot_mode_active` via Supabase REST for pilot/passenger mode testing. |
| `app-02-usage` | `kenlyle+2@gmail.com` (real) | Shallow smoke test across Dashboard/Generate/Gallery/Profiles/Settings/Billing — mostly "does the heading render," not deep per-feature verification. |
| `app-03-vault` | `kenlyle+2@gmail.com` (real, ~8 images) | Media Vault regression: filters, hover actions, search, detail dialog, a real archive→unarchive round trip verified against filtered views. |
| `app-04-mobile` | `kenlyle+2@gmail.com` (real) | Mobile (390×844) smoke + horizontal-overflow + clipped-content scan + real pixel-diff visual regression (restored 2026-08-08) across public and authenticated pages, plus a Pilot-vs-Passenger "Save to Library" visibility check. See the full write-up below. |
| `app-05-pricing-routing` | Anonymous (+ mode-aware generate check) | Routing/redirect correctness, login CTA links, `/pre-checkout` content, `woo-sync` webhook contract-only check (no live credentials asserted). |
| `app-06-network-resilience` | `kenlyle+2@gmail.com` (real) | Real generation → forced 15s offline → reconnect → responsiveness check. Duplicate-generation-prevention check is soft (logs a note, doesn't fail, if no warning appears). |
| `app-07-mobile-layout` | `kenlyle+2@gmail.com` (real) | The rigorous version of the mobile overflow check — scans every DOM element for right-edge viewport bleed (not just `body.scrollWidth`), reports offending elements/classes, across more pages than app-04 (including the Generate "Creative Direction" panel and `/profiles/new`). |
| `app-08-billing-coverage` | `kenlyle+2@gmail.com` (real, admin) | Pricing public access, Pro Plus removal, Pro SKU display (PG-PRO-N), billing events tab visibility, extra posts pill (weak — accepts either outcome for admin), variable SKU webhook DB write. |
| `app-09-purchase-funnel` | `kenlyle+2@gmail.com` (real) / anonymous | `/sign-up` email capture form, known/unknown email routing, `POST /api/auth/pre-checkout` API contract, `/login?next=` redirect, live `/api/auth/claim` + WooCommerce mirror check. |
| `app-10-usage-credits` | `kenlyle+2@gmail.com` (real, pre-seeded 3-cycle credit history required) | Credit Usage card mechanics: current-cycle progress bar, prior-cycle expand/collapse, `GET /api/usage/credits` shape. **Hardcoded local Chromium binary path** — fragile if the Playwright cache version changes. |
| `app-11-credit-statement` | `kenlyle+2@gmail.com` (real, same seeded data as app-10) | Content-fidelity companion to app-10: raw DB operation keys never leak into the UI, friendly labels appear, signed credit amounts, human-formatted prior-cycle dates, transaction drill-down. Same hardcoded-Chromium-path fragility as app-10. |
| `app-12-butler-e2e` | `kenlyle+2@gmail.com` (real, landscaper, Pro tier) | 8-phase Butler backlog flow: seed ideas → score via `/api/butler/run` → banner visible in Pilot mode → open ButlerReviewSheet → swipe right (approve, DB asserted) → verify `butler_reasoning` → swipe left (skip, `butler_skip_count` + `butler_skip_until` DB asserted). Password is hardcoded in the file rather than pulled from `.env.local` like every other script. |
| `app-13-video-reel` | `kenlyle+2@gmail.com` (real) | Real Make-Reel-to-Facebook pipeline via direct API calls (bypasses the Turnstile-blocked video UI): generate video → poll for `video_status='completed'` → publish to Facebook via PostforMe → real post ID returned. Live-cost test; depends on a specific hardcoded idea ID that may already have a completed video from a prior run, silently skipping re-generation. |
| `app-14-onboarding-usertour` | fresh disposable email each run | Full free-trial → onboarding → claim → UserTour SDK check (`window.usertour.isIdentified()`), plus a live UserTour REST API trait check if creds are set. The one script that explicitly documents defaulting to **production** so recorded runs don't need "this is just my machine" caveats. |
| `app-15-gmb-connect` | `kenlyle+2@gmail.com` (real) | Infrastructure checks for Google Business Profile OAuth connect flow: 401 when unauthenticated, valid Google OAuth URL returned authenticated, callback error paths redirect correctly, social settings page renders GMB row, Connect button initiates OAuth navigation. Does not test a completed OAuth exchange. |
| `app-16-guided-tour` | `kenlyle+2@gmail.com` (real) | AI "Get help" guided-tour widget: 401 unauthenticated, popover opens, real live-LLM question produces a real spotlight tour overlay with working Next/Done navigation — exercised 3 times with different real questions. |
| `app-17-autonomous-e2e` | `kenlyle+N@gmail.com` (temp) | The single most comprehensive test in the repo: free-trial signup → onboarding → claim → enable autonomous → real competitor discovery → real orchestrator run (~$0.05/run in Gemini tokens) → poll `batch_generation_jobs` → verify `generated_ideas`/`week_packs` → render the real pack page → verify Batch Rationale reflects real upstream signals (several checks are soft/logged-warning rather than hard-failing). Hardcoded to **production**, self-cleaning. `--dry-run` skips the costly orchestrator call. |
| `app-18-calendar-day-zoom` | `kenlyle+2@gmail.com` (real) | **Has zero assertions.** Opens the calendar, clicks a day cell, and only `console.log`s/screenshots the result for a human to read — cannot fail. Looks like a leftover diagnostic script, not a maintained regression test. |
| `app-19-calendar-popup-close` | `kenlyle+2@gmail.com` (real) | Real regression: the calendar event popover's close button must stay in-viewport on a short mobile screen. Falls back to a static HTML fixture if the account has no scheduled posts — a pass in that case reflects only the fixture, not real data. |
| `app-20-new-composite-routes` | `kenlyle+2@gmail.com` (real, 2 hardcoded profiles) | Live-cost coverage of two previously-untested routing paths: WORKSPACE_REMIX (via the real Passenger `/generate` flow) and MULTI_IMAGE_COMPOSITE ("AI Assisted"), each checked for correct `routing_key`, correct model tier, and an AI-judged coherence pass on the real generated image. **Headed by default** ("Ken wants to watch these run") — pass `--headless` to opt out. |
| `e2e-generate` | `kenlyle+2@gmail.com` (real, 2-profile) | Full week-pack generation E2E for all modes (pilot/passenger) × routes (trivial/mixed/edge/story/vault/edit-from-pack); profile auto-selection, Architect scan, TinderReview, pack assembly, PostforMe calendar verification. Prod-by-default, headed-by-default, publishes by default. |
| `e2e-partial-week` | `kenlyle+2@gmail.com` (real) | Partial first-week onboarding: asserts Architect plan and pack item count ≤ days remaining in the current `week_starts_on`-aligned week; self-adjusts expected count by running day. |
| `run-all` | n/a (orchestrator) | Master runner — sequentially executes the named `.cjs` test scripts, streams live output, rolls up pass/fail. Has no assertions of its own; only as good as the scripts it calls. |
| `run-regression-suite` | n/a (orchestrator) | Master runner for the `onboard-05` through `onboard-13` / orchestrator / pack-assembly batch of verification scripts, with a rolled-up pass/fail summary. |
| `prod-01-generate-flow` | production account | Production-only Playwright smoke suite with real `assert()` calls — deliberately targets prod because Turnstile and live image-gen can't be exercised on localhost. |
| `verify-l18-seasonal-optimizer` | n/a | Unit-level real-assertion test of the pure `optimizePublishTimes()` scheduling function (complements `onboard-10`'s live-route test of the same feature). |
| `verify-l20-readiness-queries` | seeded live profile | Real assertions verifying the exact Supabase queries the dashboard readiness checklist depends on. |
| `verify-l36-phase-ab-live` | live `business_profiles` rows | Live onboarding-pipeline test, 2 cases, explicit ok/fail assertions, includes cleanup. |
| `verify-l36-phase-c-trial-live` | anonymous trial funnel | Playwright-driven anonymous trial funnel test with assertions at every step plus a login-redirect regression check. |
| `niche-intelligence-01-goth-tattoo` | real generated content | Acceptance test with a defined, code-checked acceptance bar (vocabulary hit-count, word-count limits) against real AI-generated content — not an LLM's subjective opinion. |
| `test-text-legibility-fallback-202607` | n/a (unit, direct import) | 4 explicit assertions calling the actual production `detectImageDefect`/`resolveDefectFallback` functions directly (imported, not reimplemented). |
| `social-01-fb-publish` | `kenlyle+2@gmail.com` (real) | End-to-end Facebook publish: generate idea, save, select page, publish, verify success badge. Most of Suite 3 silently skips if Facebook isn't connected, and the AI-generation step can silently no-op in headless mode ("Turnstile not bypassed"), which cascades to skipping save/publish too. |
| `social-02-api-contract` | `kenlyle+2@gmail.com` (real) | Headless `fetch`-only contract tests for social publish endpoints: 401, 405, 422, response shape. Status-code/shape validation only, not business-logic verification. |
| `social-03-token-health` | `kenlyle+2@gmail.com` (real) | Supabase-admin audit of OAuth token health: refresh_token presence, expiry windows, schema compatibility. Silently skips a platform entirely if the account has zero connections for it. |
| `social-04-multi-publish` | `kenlyle+2@gmail.com` (real, needs FB+IG both connected) | Four suites: API shape check (both channels), UI connected-status check, real pack schedule-drawer multi-channel selection, and (unless `--skip-schedule`) a real live schedule-then-delete round trip against actual PostforMe/calendar state — the most functionally meaningful of the four social scripts, but gated on a pre-existing "ready" pack existing. |
| `billing-01-woo-webhook` | `kenlyle+2@gmail.com` (real, state-mutating) | WooCommerce/Bit-Integrations webhook auth + contract against the live deployed function: 401/405 checks always run; the functional cancel/idempotency/unknown-customer checks only run if `WOO_WEBHOOK_SECRET` is set locally. |
| `billing-02-charge-atomic` | `kenlyle+2@gmail.com` (real, state-mutating, restored after) | Direct RPC regression test of `charge_credits_atomic` against two named historical bugs: sequential-charge accumulation and stale-cycle rollover, plus a live pricing-formula check and an idempotency-key check. Real functional coverage of real billing logic, not a shape check. |
| `postforme-audit` | none (env: `POSTFORME_API_KEY`) | Lists all PostForMe social accounts with `id`, `platform`, `status`, `external_id`, and `username` — diagnostic utility for verifying account sync state and connection health. Read-only, no assertions. |

**Self-cleaning:** all three `onboard-*` scripts delete the test user from `auth.users` at the end of every run via the admin API (try/finally, so cleanup runs even on failure). Requires `SUPABASE_SERVICE_ROLE_KEY` in `.env.local`; skips gracefully if absent.

**DB isolation:** all authenticated tests that mutate data on `kenlyle+2@gmail.com` now call `cleanupTestUserData` from `scripts/_shared/teardown.cjs` before the suite starts and after any fatal error. This prevents credit exhaustion and `generated_ideas` table bloat from repeated runs.

---

## Shared Utilities

### `scripts/_shared/teardown.cjs`

Uses the Supabase service role to delete test-generated rows for a given email that were created within the last 2 hours. Cleans in dependency order:

1. `week_pack_items` (child of week_packs)
2. `week_packs`
3. `generated_ideas` (only `discover` and `approved` stages — curated content is never touched)
4. `generated_idea_batches`

**Integrated into:** `app-02-usage`, `app-03-vault`, `app-04-mobile`, `social-01-fb-publish`, `app-06-network-resilience`.

**Why this matters:** Before the isolation helper, repeated runs of `app-02-usage` consumed generation credits and left rows in `generated_ideas`. After a week of daily CI, the account would hit its credit limit and every subsequent run would fail with "No credits." The helper makes the test suite idempotent.

```bash title="Audit only (dry run)"
node -e "require('./scripts/_shared/teardown').auditTestUserData('kenlyle+2@gmail.com')"
```

### `scripts/_shared/visualRegression.cjs`

Reusable pixel-diff wrapper around `pixelmatch` and `pngjs`. Any script can add visual regression in three lines:

```javascript
const { runVisualCheck } = require('./_shared/visualRegression');
await page.screenshot({ path: '/tmp/current.png' });
const result = runVisualCheck('/tmp/current.png', './baselines/expected.png', '/tmp/diff.png');
if (result.status === 'fail') throw new Error(result.reason);
```

**API:**

| Export | Purpose |
|---|---|
| `diffImages(current, baseline, diff, options)` | Core pixel comparison. Returns `{ match, diffPixels, diffPercent, reason }`. |
| `seedBaseline(current, baseline)` | Copies current screenshot to baseline directory. |
| `runVisualCheck(current, baseline, diff, options)` | One-liner: seeds baseline if missing, otherwise diffs and returns `{ status: 'baseline'\|'pass'\|'fail', ... }`. |
| `DEFAULT_THRESHOLD` | `0.001` (0.1% diff) — import and override per-test. |

### `scripts/_shared/resilient.cjs`

Automatic retry wrapper for Playwright navigation, built for intermittent connectivity.

```javascript
const { gotoWithRetry } = require('./_shared/resilient');
await gotoWithRetry(page, `${BASE}/generate`, { waitUntil: 'networkidle', timeout: 30000 });
```

Retries up to 2 times on transient network errors (`ERR_CONNECTION_RESET`, `ERR_NETWORK_CHANGED`, `ERR_ABORTED`, `Timeout exceeded`). Uses 1.5s × attempt backoff. Integrated into all authenticated E2E scripts that navigate after login: `app-02-usage`, `app-03-vault`, `social-01-fb-publish`, and `app-06-network-resilience`.

**Why this matters:** Before the resilient helper, a single packet-loss spike during `page.goto()` in Costa Rica would fail the entire test suite. Now the suite auto-recovers from transient network hiccups without human intervention.

### `admin-reset-usage.cjs` (postglider-auto repo root — not `scripts/`)

Zeros a user's daily/monthly usage counters via the **real** admin endpoint
(`PATCH /api/admin/user-search`), driven through the actual `/admin` UI — not a raw database
write. Built 2026-07-03 after `kenlyle+2@gmail.com` hit its monthly quota mid-session during manual
feature testing (a scenario the `teardown.cjs`/`cleanupTestUserData` isolation above doesn't cover,
since that cleans up *generated rows*, not the usage counters themselves).

```bash
node admin-reset-usage.cjs <target-user-email> [admin-email] [--dev]
```

**Why not just `UPDATE profiles SET usage_monthly = 0` with the service-role key?** That skips
`verifyAdmin()`'s `is_admin` check, the endpoint's own field handling, and any audit trail a real
admin action leaves. This script instead logs in **as a real admin account** (default
`kenlyle@gmail.com`) via a passwordless Supabase magiclink exchange, then clicks the actual
**Admin** (sidebar) → **Admin Actions** (tab) → look up email → **Zero both** → **Save & stamp
reset date to today** buttons — the same clicks a human admin makes.

<Callout kind="warning">
  Never modify this script to `console.log` the callback URL, the raw session fragment, or
  `page.url()` right after navigating to it — all three carry a live bearer token for the admin
  account. An earlier version of this exact script did this for diagnostics and printed a live
  access token into a Claude Code transcript (2026-07-03). Extract tokens in memory only and pass
  them straight into `page.goto()`.
</Callout>

**Fixed 2026-07-03:** the Supabase project's Auth redirect-URL allow-list originally only
recognized `app.postglider.com` (the retired core app), which is why the script builds its own
same-domain callback URL from the session fragment instead of trusting Supabase's `redirect_to`.
`https://autonomous.postglider.com/auth/callback` and `https://autonomous.postglider.com/` have
since been added to the allow-list, so `redirect_to` works correctly now too — the workaround
stays in place since it's harmless and more explicit about which URL gets used.

### `test-single-post.cjs` (postglider-auto repo root — not `scripts/`)

The cheapest possible real-pipeline test: exactly **one** manual post box (WITP pick → AI Image
merge → Save Post → Create My Batch), no orchestrator, no candidates, no hybrid batch, no backlog.
Built 2026-07-24 while verifying the WITP identity-lock fix; kept in the toolbox as a reusable
spot-check for any single-image generation-path change, since it burns one generation instead of
a full Ultimate Batch worth of Replicate/Riverflow credit.

Logs in via the same passwordless magiclink mechanism as `admin-reset-usage.cjs`, then drives the
real `/generate` UI directly with Playwright: select profile → batch size 1 → pick a "Who's in
this post?" chip → expand a content category → pick an item chip → type post direction text →
Add to Post → AI Image → Save Post → Create My Batch → poll for the generating spinner to clear.

```bash
node test-single-post.cjs [options]
```

| Flag | Default | Purpose |
|---|---|---|
| `--email <email>` | `notmichaelbrown@gmail.com` | Login email |
| `--profile <label>` | `Stay True Tattoo` | Business profile visible-text label to select |
| `--witp <name>` | any non-"Mike Brown" chip | Substring to match the WITP chip by — the default deliberately avoids the default identity, to prove a WITP fix isn't just falling back to it |
| `--category <label>` | `Story-Based` | Top-level content-direction category to expand. Pass an empty string to use a top-level freeform chip instead (`--item` is ignored in that case) |
| `--item <label>` | `A Lesson I Learned This Week` | Specific item chip under that category |
| `--text <text>` | pickle-jar test line | Post direction text typed into the field |
| `--headless` | off (headed) | Run headless instead of the default headed window |
| `--dev` | off (production) | Target `localhost:3000` instead of `autonomous.postglider.com` |
| `--max-wait <seconds>` | `180` | How long to wait for generation to finish before giving up |

**When to reach for it:** a fast, cheap spot-check when only one generation path needs verifying
(e.g. a WITP/identity fix, an equipment-routing change, a prompt tweak) — instead of running a
full Ultimate Batch just to exercise one image.

<Callout kind="warning">
  Same rule as `admin-reset-usage.cjs`: never `console.log` the magiclink callback URL or
  `page.url()` right after navigating to it — both carry a live bearer token.
</Callout>

### `review-pack-images.mjs` (postglider-auto repo root — not `scripts/`)

Downloads every item in a real pack (image + caption + category) to a local directory named by
`batch_alpha_id`, for actually looking at each image next to its caption — not just confirming a
URL resolves or an automated defect check passed. Built 2026-08-20 reviewing Kelleen Gebler's
batch, which passed every automated check (solid-fill detector, Editor Agent) while two posts
used the exact same thematically wrong stock photo (a beach cabana image on a post about local
New Hope pride, and another about a team's morning ritual) — a real content-coherence mismatch no
pixel-variance or LLM-redundancy check was checking for at all.

```bash
npx tsx --env-file=.env.local review-pack-images.mjs <packId> [outDir]
```

Prints each post's caption/category, saves its image as `<outDir>/<letter>.png` (default
`./pack-review-<packId prefix>`). Then `Read` each saved image directly — this script only
fetches and organizes; the actual judgment of "does this image match this caption" still needs a
real look, human or Claude.

### Kelleen Gebler demo diagnostic scripts (postglider-auto repo root)

Built 2026-08-19 while getting Kelleen's ("Go To Concierge, LLC",
`0e97f49f-84b4-4d16-92b3-be9a08a791d9`) demo profile ready for a real Ultimate Batch — a mix of
one-off repro tools and reusable route-callers, all hardcoded to her profile ID. Kept as saved
tests rather than deleted since the same profile is likely to need re-poking before her sales
conversation closes.

| Script | What it does |
|---|---|
| `cleanup-and-discover-kelleen.mjs` | Deletes the garbage `competitor_accounts` rows that had accumulated for her profile (unrelated businesses — tattoo studios, a hiking overlook), then re-runs the real discovery route (`POST /api/autonomous/prefetch/discovery`) to repopulate it cleanly. |
| `debug-niche-assist-codes.mjs` | Diagnostic: reads her `classification_codes`/services from the DB, then calls the real `generateNicheAssist()` directly (not via the route) with the same NAICS/SIC targets and business context production would use, to see per-code errors that the route's own error handling would otherwise swallow. |
| `debug-niche-assist-raw.mjs` | Diagnostic: wraps global `fetch` to log the raw OpenAI response status/body when `generateNicheAssist()` calls `api.openai.com/v1/chat/completions`, since `callNicheAssist()` isn't exported for direct inspection. |
| `refresh-niche-assist-kelleen.mjs` | Calls the real `POST /api/generate-niche-assist` route for her profile — the production path, not a reimplementation. |
| `trigger-signal-detection-kelleen.mjs` | Calls the real Virality and Influencer prefetch routes (`POST /api/autonomous/prefetch/virality`, `.../prefetch/influencer`) for her profile, in sequence. |
| `generate-robinreach-comparison-pdf.mjs` | Generates `docs/postforme-replacement-vendor-comparison-2026-08-18.pdf` — a vendor-comparison report evaluating RobinReach as a PostForMe replacement, substantiating PostForMe's failure to deliver value. No `lib/` imports, pure HTML→PDF via `playwright-core`. |
| `open-stepcapture-as-mike.cjs` | Opens a real headed browser logged in as Mike Brown **with the StepCapture extension actually loaded** — unlike `view-as-mike.cjs`, which launches a plain `chromium.launch()`/`newContext()` that Playwright defaults to running with `--disable-extensions`. Mirrors `stepcapture-ultimate-batch.cjs`'s real `launchPersistentContext()` config. Same never-log-the-callback-URL/`page.url()` rule as `admin-reset-usage.cjs`. |
| `regen-kelleen-half-image.cjs` | Logs in as a real user via magiclink, navigates to a given pack, then calls the real `POST /api/packs/[packId]/generate-images` for a list of idea ids in sequence (real cookies/session, not a service-role bypass) — built chasing the 2026-08-19/20 "half image" solid-fill defect chain, generalized to accept any list of idea ids/pack id rather than hardcoded to one. |

### `regenerate-pack-rationale.mjs` (`pg-projects/scripts/autonomous/` — shared, not postglider-auto's own)

Rebuilds and re-saves an already-created pack's stored "Why this batch" rationale using the
current `buildBatchRationale()` logic, without re-running a full (paid, content-generating,
auto-publishing) autonomous batch. Built 2026-07-13 after Ken caught two problems in a real
rationale: a QUOTA count that silently excluded additive dedicated posts, and a schedule
explanation truncated mid-word. Fixing the generation logic alone doesn't help an *existing* pack
— the pack page only ever reads whatever rationale was stored at generation time — so this script
is the repeatable way to apply a rationale-generation fix retroactively.

```bash
node scripts/autonomous/regenerate-pack-rationale.mjs <packId> [--dev]
```

Hits `POST /api/autonomous/regenerate-rationale` in `postglider-auto` (Bearer `CRON_SECRET`).
Ground-truth data (schedule, post content, dedication sources) is read fresh from
`week_pack_items`/`generated_ideas`; quota counts and trending hooks are parsed back out of the
pack's *prior* stored rationale, since those describe a decision made at generation time and
aren't persisted anywhere else; testimonials and the schedule reasoning are re-fetched live.

---

## Chain Reasoning — Why This Order

**`onboard-01/02/03/04`** exercise the same `/start` funnel with four different business types (tattoo studio, construction, pizza/restaurant, generic trial), giving breadth across enrichment categories. Fake accounts mean no leftover data accumulates; teardown ensures each run starts clean.

**`app-01-ux` → `app-02-usage`** both run on `kenlyle+2@gmail.com`. `app-01-ux` is the tighter, faster regression check (23 named assertions); `app-02-usage` is broader and verifies the same surfaces more loosely. Together they are the "does the app basically work" check before going deeper.

**`app-02-usage` → `app-03-vault`** is the natural handoff: `app-02-usage` confirms the gallery page loads; `app-03-vault` then goes 22 sub-tests deep into it. Same account, same ~8 images — run back-to-back for full gallery coverage.

**`app-04-mobile`** now sits at the end as a visual regression gate, not just a spot-check. It seeds baselines on first run and fails on any subsequent run where a page differs by more than 0.1% of pixels. This catches CSS layout regressions that functional assertions miss.

**`app-05-pricing-routing`** runs anonymously — no login required — verifying routing logic, pricing webhooks, and IdeaCard schema changes.

**`app-06-network-resilience`** is the infrastructure stress-test. It validates the exact async interaction (frontend timeout ↔ Realtime subscription ↔ webhook arrival) that no other test covers. Runs last in the app group because it manipulates browser connectivity state and takes the longest.

**`app-12-butler-e2e`** is the integration test for Butler — the AI backlog scoring agent. It runs after the core app tests because it depends on `kenlyle+2@gmail.com` having a Pro tier and existing `business_profiles` rows (verified by `app-01-ux` and `app-02-usage`). The 8-phase structure mirrors the user journey: seed data → score → UI surface → approve → skip. DB assertions via service role make the pass/fail binary — not subject to timing or UI rendering quirks.

**`social-01-fb-publish` → `social-02-api-contract` → `social-03-token-health`** form the social-publishing validation stack:

1. `social-01-fb-publish` is the slow, expensive E2E (~90s): real browser, real generation, real API call to Facebook.
2. `social-02-api-contract` is the fast, cheap validation (~5s): headless `fetch` calls that verify status codes and response shapes. If someone changes the error response from `{ error: string }` to `{ message: string }`, this catches it in milliseconds.
3. `social-03-token-health` is the proactive monitor: it queries `social_connections` via Supabase admin to warn if any OAuth token expires within 7 days.

---

## `postglider-uilicious-tests` — the human-editable test layer

Separate repo: `github.com/kenlyle2/postglider-uilicious-tests` (private). Real `I.click()`/
`I.see()` Uilicious tests, run against the same live "PostGlider" Uilicious cloud project as
`tests/e2e-integrated/phase3-uilicious.ts` below. Deliberately distinct from the `#region fz-<id>`-
marked Playwright scripts in this repo — those markers serve Claude/dev traceability; Uilicious's
plain DSL is the layer meant for a non-developer to read or hand-edit directly (add a wait,
comment out a step), with a scheduled GitHub Actions sync (`sync-uilicious-saves.cjs`, every 15
minutes) pulling any Studio-side "Save" back into git as the safety net. Full detail — including a
real credential-leak-and-rotation incident from building it — lives in `decisions.md`
D-2026-08-08l and the new repo's own README, not duplicated here.

**Rows implemented (2026-08-10): fz-015 through fz-018** — Fill Viral / Fill Influencer / Fill
Offer / Fill Backlog, all against `kenlyle+2@gmail.com`'s "Well Pruned Acres" profile. Two real,
disclosed fixes came out of adding fz-017/018:
- `/generate` defaults to the business profile with the most recent `created_at`, not a fixed
  one — this account has 4 profiles, so "Well Pruned Acres in view" needed Well Pruned Acres'
  `created_at` bumped to `now()` to stay the durable default. Uilicious's own `I.select()` cannot
  reliably switch a native `<select>` to a non-default option in this project's cloud runner
  (confirmed a platform limitation, not a syntax bug — the identical `I.select(label, option)`
  pattern works elsewhere in this project, and re-selecting the already-selected option
  "succeeds," but switching to any other option consistently errors).
- The Backlog picker (fz-018) only renders inside the "Weekly Architect" card, which only renders
  in **Pilot Mode** — a per-account toggle on `/settings` (`PilotModeCard`), not `/generate`
  itself. `app/(app)/generate/page.tsx`'s own `togglePilotMode` callback and `pilotEligible` state
  are dead code (defined, zero call sites) — Pilot Mode can only actually be turned on via
  Settings. `kenlyle+2@gmail.com`'s `profiles.is_pilot_mode_active` was set to `true` directly
  (the same column the real Settings toggle writes) as a one-time account-level test prerequisite.
  Also added `data-testid` attributes (`pin-from-backlog-button`, `backlog-idea-card`,
  `backlog-confirm-button`) to `components/generate/BacklogPickerModal.tsx` /
  `WeeklyArchitectCard.tsx` (`postglider-auto` commit `ad00c91f`) so fz-018 didn't need a fragile
  positional CSS selector to pick "the first idea."
- A second password leak happened building these: `uilicious-cli project run --json` also echoes
  the literal password value into its step output (not just the default non-JSON mode already
  documented in D-2026-08-08l) — `--json` is **not** a safe mitigation for this CLI. The password
  was rotated twice (once for a misfired API call that hit an unrelated anonymous auth row by
  mistake — no real impact, that row has no email/password login path — then correctly for the
  real account), both leaked values confirmed dead via a direct token-grant probe.

---

## Purchase-to-Production Integrated Test Suite (`tests/e2e-integrated/`)

**Added since the last verification (this doc previously had no record of this suite at all).** This is the newest, most rigorous test in the repo — a real, live, 5-phase purchase-to-production chain that replicates what an actual paying customer experiences, built per `.dwp/plans/2026-07-15-e2e-integrated-test-suite.md`.

```bash
npx tsx tests/e2e-integrated/run-all.ts [--skip-uilicious] [--skip-content-gen]
```

`run-all.ts` runs all 5 phases sequentially, aggregates PASS/FAIL, and produces a PDF report via `scripts/_shared/make-pdf-report.cjs`. `--skip-uilicious` skips Phase 3's Uilicious half (costs real Uilicious cloud minutes); `--skip-content-gen` skips Phase 4 (hits real Gemini/OpenAI — real cost).

| Phase | What it does |
|---|---|
| **1 — Order placement** (`phase1-order-placement.ts`) | Browser-driven "Place Order" was confirmed unachievable (`decisions.md` D-2026-07-15e/f/g), so this replicates a real PG-PRO-3 purchase's downstream effects via WP-CLI instead: creates a real WordPress user, `wp wc shop_order create`s a real order, then `wp pg-e2e process_order` fires the same `woocommerce_checkout_order_processed`/subscription-activation events a real checkout does — triggering the real Bit Integrations webhook that provisions the Supabase profile. |
| **2 — DB state verification** (`phase2-db-verification.ts`) | Queries the real Supabase profile Phase 1 provisioned and asserts tier/profile-cap/posts-remaining against the **real enforcement-twin functions** (`resolveMonthlyLimit`, `creditsToPosts` — imported directly from `lib/billing/limits.ts` and `lib/utils/credits.ts`, not reimplemented), so the test can never drift from the actual billing math. |
| **3a — UI truth (Playwright)** (`phase3-posts-math.ts`) | Logs in via magic link (never logs the token), screenshots `/billing`, and compares the displayed "N posts left" against Phase 2's computed value (±1 tolerance for rounding). |
| **3b — UI truth (Uilicious)** (`phase3-uilicious.ts`) | Runs the real Uilicious cloud "PostGlider" project's `Billing And Profiles.test.js` against a live magic-link session — verifies tier label, profile creation up to the 3-profile cap, and that a 4th attempt is correctly blocked. |
| **4 — Content generation quality** (`phase4-content-generation.ts`) | Extends `scripts/e2e-generate.cjs` (via its `--profile-id`/`--profile-name` flags) rather than reimplementing generation, and verifies `usage_monthly` increased and `/billing`'s displayed posts-remaining decreased by the matching amount. |
| **5 — Subscription lifecycle** (`phase5-subscription-lifecycle.ts`) | Simulates the real WPSubscription/Bit Integrations webhook payload shape (captured live during this suite's own development, not guessed) directly against `app/api/wpsubscription-webhook` — cancelled → re-verify DB → active (reactivate) → re-verify DB. Tests the webhook receiver in isolation, distinct from Phase 1's real WordPress-side trigger. |

**Test data convention:** disposable `kenlyle+NN@gmail.com` addresses, allocated sequentially via `lib/emailSequence.ts`. Default: no cleanup — test-user history is left for audit.

**Uilicious fixture files** (`tests/e2e-integrated/uilicious/*.test.js`) also include standalone navigation/mobile-overflow/login-flow smoke tests (`Navigate to Billing`, `Sidebar Navigation Mobile`, `Mobile Layout — No Horizontal Overflow`, `Gallery Search Mobile`, `Login Flow`, etc.) that can be run independently via the Uilicious cloud project, plus `Checkout - Pro Subscription.test.js` — a real live WooCommerce/Stripe checkout against `postglider.com` using a disposable Wise card and a `$0.50`-net coupon (same expired-coupon caveat as `live-checkout-dry-run.cjs` below — check the coupon is still valid before relying on this one). One file, `Sign Out.test.js`, is a **leftover Uilicious template** (still references `github.com/login` / `johndoe` placeholder credentials) — it is not a real test and should not be trusted as sign-out coverage.

A real run of this suite has previously completed successfully end-to-end (see `tests/e2e-integrated/.runs/99e61ecd-...json` — a passed run for `kenlyle+72@gmail.com`, tier `pro`, 3 profiles allowed), confirming this isn't just a plan on paper.

---

## Root-Level Admin & Verification Scripts (`postglider-auto/` repo root)

These live directly at the repo root (not in `scripts/`), and split into two kinds: reusable QA/admin tools (drive real UI or real functions, no fabricated data) and point-in-time regression checks written to confirm a specific shipped fix still holds.

| Script | What it does | Invocation | Account |
|---|---|---|---|
| `chaos-onboarding-walkthrough.cjs` | Structured critical-path regression, built 2026-08-07 after the PO pushed for manual click-testing: verifies every dashboard onboarding-checklist link actually navigates somewhere real, then does semi-random careless things to a real profile (max out the Owner Appearance % slider by mouse AND by keyboard-only, delete+re-add a classification code, confirm a known duplicate-worker data gap) — all destructive steps snapshot/restore, safe to run against production repeatedly. Found and led to fixing 3 real bugs same-day (keyboard-only slider commit, exact-code search, Denys/Denis duplicate). Runs **daily and unattended** via `pg-admin`'s `postglider-auto-test-runner` Cloud Run Job (a dedicated job, sharing an image with the fuzzer's own job below) + Cloud Scheduler (`postglider-auto-chaos-regression-daily`, `0 13 * * *` UTC) — see `pg-admin/CLAUDE.md`'s Tenants list. (A same-day-deleted GitHub Actions version of this schedule was replaced with this real, already-existing pattern.) | `node chaos-onboarding-walkthrough.cjs [--prod] [--headless]` | `kenlyle+2@gmail.com` (magic link) |
| `chaos-fuzzer.cjs` | Right-sized randomized fuzzer, Phase 2 of the same effort — a weighted random pool of ~14 action types (navigation chaos, tab-switching, garbage-text paste, WITP flapping, glossary add/delete, role-cycle spam, Niche DNA search, owner-% slider abuse, and real calendar drag-and-drop) repeated to a configurable action count. Hard exclusion list (never clicks Delete Account/Sign out/Disconnect/Connect-OAuth/Publish/Checkout), bounded occurrence counts on anything that mutates real data, and a DB-verified restore check specifically for calendar drags (fails loudly on any mismatch, since that's a real customer's real schedule). Draws its action pool from `docs/CHAOS-ACTION-CATALOG.md`'s 365 code-grounded candidates. **Verified clean at full 300-action scale 2026-08-08** (found and fixed two real harness bugs in the process — see `decisions.md` D-2026-08-08b) and now runs **weekly and unattended** via `pg-admin`'s dedicated `postglider-auto-test-runner-fuzzer` Cloud Run Job + Cloud Scheduler (`postglider-auto-fuzzer-weekly`, `0 14 * * 0` UTC). | `node chaos-fuzzer.cjs [--actions 300] [--prod] [--headless]` | `kenlyle+2@gmail.com` (magic link) |
| `admin-reset-usage.cjs` | Reusable admin tool — drives the real `/admin` Admin Actions UI (not a raw DB write) to zero a user's usage counters. See full write-up above. | `node admin-reset-usage.cjs <target-email> [admin-email] [--dev]` | `kenlyle@gmail.com` (magic link) |
| `verify-gmb-connect.cjs` | Point-in-time regression, built 2026-08-18 verifying the GMB OAuth credential re-issue (`decisions.md` D-2026-08-18g — `GOOGLE_CLIENT_ID`/`SECRET` had been issued under a legacy, billing-disabled `kenlyle@gmail.com` GCP project, same pattern as the `GOOGLE_PLACES_API_KEY` incident). Logs in via magiclink, calls the real `GET /api/gmb/connect` route with that session, and inspects the returned Google OAuth authorization URL's `client_id`/`redirect_uri` against the expected `postglider-prod` values — this is how a second, unrelated bug (Cloud Run's `GOOGLE_REDIRECT_URI` still pointing at the retired `autonomous.postglider.com` domain, missed by an earlier domain-flip cleanup pass) was caught before a real user hit it. Only verifies the authorization-request half — the actual Google consent round-trip needs a real human click-through with a test-user Google account, not scriptable. | `node verify-gmb-connect.cjs [admin-email]` | `kenlyle@gmail.com` (magic link), prod only, no DB writes |
| `view-as-mike.cjs` | Reusable QA tool — opens a persistent, real, headed browser logged in as the real customer account for manual inspection of any page/pack. No assertions of its own. | `node view-as-mike.cjs [path-or-full-url]` | `notmichaelbrown@gmail.com` (magic link), prod only |
| `view-as-user.cjs` | Reusable QA tool, same magiclink mechanism as `view-as-mike.cjs` but for ANY target email — use this instead of writing a new hardcoded one-off. Default mode opens a persistent headed browser for manual inspection. `--screenshot <file.png> --headless` mode (added 2026-08-16 for unattended XPRIZE #6 evidence capture) logs in, navigates, takes one full-page screenshot, and exits cleanly instead of staying open — no interactive session needed. | `node view-as-user.cjs <email> [path-or-full-url]` · `node view-as-user.cjs <email> [path] --screenshot <file.png> --headless` | any target user (magic link), prod only |
| `stepcapture-ultimate-batch.cjs` | Reusable marketing/demo capture tool, built 2026-08-12 for the "zero to Ultimate Batch" XPRIZE/marketing video — drives a genuinely fresh onboarding signup (business name + city only, no pre-existing account) through real GMB import, real website team-mining, Owner Appearance % setting, and a real Ultimate Batch (Autonomous Batch → real Orchestrator-generated ideas → second-Tinder approve loop, ported from `scripts/e2e-generate.cjs`'s `driveTinderReview`/`clickOrAcceptNav` pattern → real assembled pack), with the StepCapture Chrome extension loaded and recording every real step. Found and fixed 4 real production bugs chasing a clean run: a Google Maps enrichment fallback returning literal "Google Maps" as the business name (`lib/integrations/businessEnrichment.ts`, since a raw Maps deep-link's un-rendered HTML shell was being GPT-scraped as if it were real page content), the resulting NOT NULL `business_profiles.industry` constraint violation, a stale WP-SSO `supawp_app_callback_url` bouncing new signups to the wrong domain post-domain-flip (fixed via WP-CLI), and 3 vendor-name leaks (GPT-4o, Instig8.AI, Apify, OpenAI) in onboarding/marketing copy. Ends with a slow eased scroll down the finished pack page for the recording. `--no-stepcapture` (2026-08-14) skips the extension entirely, driving the real candidate-Add/Autonomous-Batch-trigger clicks safely (StepCapture's capture-phase click interception races against them — D-2026-08-14l) while still producing plain Playwright `.webm` video segments; `--resume <N>` (real password login) resumes that run's account for a StepCapture-loaded tour pass instead. `--pack-only` (2026-08-15, requires `--resume`) narrows that tour pass further — even plain `--resume` still drives the full remaining flow (Owner%, tours, candidate review) and can re-hit the same click race; `--pack-only` skips straight from login to that run's already-completed pack (sourced from `RunN.json`'s `packUrl`) and does the reveal pan there only. | `node stepcapture-ultimate-batch.cjs` (fresh + full StepCapture) · `node stepcapture-ultimate-batch.cjs --no-stepcapture` (functional run, plain video) · `node stepcapture-ultimate-batch.cjs --resume <N> --pack-only` (StepCapture-safe pack-reveal pass) | fresh signup each run (`kenlyle+demo<timestamp>@gmail.com`), headed only, requires the StepCapture extension unpacked at `extensions/stepcapture/` (gitignored, not redistributed) and real fixture photos in `demo-assets/` (gitignored) |
| `bench-stale-offer-vendors.mjs` | Reusable 4-vendor comparison harness (Luna, DeepSeek, gemini-3.7-flash, gemini-3.5-flash), built 2026-08-20 to close out the Kilo Gateway evaluation for good (`decisions.md` D-2026-08-19h/i/t) — reuses the REAL production prompt/schema, extracted from `lib/autonomous/staleOfferDetection.ts`'s `checkOfferStalenessSemantic()` (`buildStaleOfferPrompt()`/`STALE_OFFER_SCHEMA_GEMINI`/`STALE_OFFER_SCHEMA_OPENAI_COMPAT`) specifically so this bench calls real production logic, not a hand-copy. 4 known-answer test cases (deliberately outside the deterministic holiday backstop, so every call reaches the real LLM path), all forced to each vendor's own non-thinking config. Found DeepSeek decisively best (4/4, ~450ms) and Luna worst (2/4) on this specific task — contradicts Luna's 5/5 win on the unrelated Marketing Agent comparison, confirming no single model wins across every call site. Also found two new `gemini-3.7-flash` landmines: `ThinkingLevel.MINIMAL` is rejected outright (`LOW` is the lowest legal value), and no genuine zero-thinking mode exists at all (even `thinkingBudget: 0` burns real thought tokens) — the model's mandatory thinking overhead silently truncated JSON output at production's normal `maxOutputTokens` budget until bumped. | `npx tsx --env-file=.env.local bench-stale-offer-vendors.mjs` | no browser, real Supabase (none — pure prompt bench), real billed API calls to OpenAI/DeepSeek/Vertex, requires local Vertex ADC (`gcloud auth application-default login`) |
| `deepseek-test-harness.mjs` | Reusable multi-model text-classification benchmark harness, built 2026-08-05, previously missing from this inventory (added 2026-08-14 closing that gap). A SUITE defines real test cases + a shared prompt + a parser; a CONFIG tests one model/setting combination against a GPT-4o-mini reference, with real per-call cost from actual API `usage` data (not estimates) and an optional blind third-party Gemini judge on disagreements. Registered candidates: GPT-4o-mini, GPT-5.6 Luna, DeepSeek V4-Flash, Grok 4.6 (`grok-4-6`, added 2026-08-14 — `XAI_API_KEY` not yet provisioned, so unused in production), plus a real-production-`generateJson()` Gemini 3.7 Flash caller. `--candidate=<json>` registers a brand-new model spec at runtime without editing the file (the Tier 1 → Tier 2 auto-wiring path from `pg-admin/scripts/text-model-monitor-agent.cjs`). Suites: `classifyBrief`, `naics`, `searchCodes`, `detectServices`, `outscraperNiche`, and `staytrueTattoo` (added 2026-08-14 — a real single-case SIC/NAICS comparison across all 4 vendors for one specific business, used to inform the `lib/genie/classifier.ts` DeepSeek-vs-GPT-4o-mini routing decision; see `decisions.md` D-2026-08-14m). Also importable in-process by `app/api/admin/model-harness-run/route.ts`. Any NEW model-comparison work should extend this file with a new suite, not write a new script. | `npx tsx --env-file=.env.local deepseek-test-harness.mjs --suite=<name> [--sample=N] [--judge] [--candidate='<json>']` | no browser, real Supabase reads (test-case sourcing only, no writes), real billed API calls to every registered candidate |
| `test-guide-me-live.cjs` | Point-in-time regression for the onboarding-checklist "Guide me" buttons (`components/dashboard/OnboardingReadiness.tsx`) — built diagnosing/verifying the 2026-08-12 stuck-dark-screen fix (`decisions.md` D-2026-08-12c). Clicks the first "Guide me" button found on `/dashboard`, captures the real `/api/autonomous/guide` request/response and any console errors, and screenshots the resulting tour state. Complements `verify-tourguide.cjs` (below), which only exercises an always-in-DOM target — this one exercises the checklist's own page-gated targets, the failure mode `verify-tourguide.cjs` missed. | `node test-guide-me-live.cjs` | `kenlyle+2@gmail.com` (magic link), tested against `localhost:3000` (guide API call itself still hits the deployed prod endpoint — `NEXT_PUBLIC_AUTONOMOUS_URL` unset) |
| `marketing-agent-model-shootout.ts` | Real model/thinking-level shootout for Marketing Agent (`lib/autonomous/marketingAgentAgent.ts`) — calls the exact production `runMarketingAgentPicksGemini`/`runMarketingAgentPicksDeepSeek` functions (extracted 2026-08-15 specifically so this bench isn't a hand-copied reimplementation) against a real captured Stay True Tattoo eligible pool. Built 2026-08-15 chasing a real "0 posts" batch failure down to Vertex DSQ contention; found Gemini 3.6 Flash taking 11+ minutes per call (and degrading from 5 requested picks to 1 under repeated 429s) vs. DeepSeek V4 Flash's 853ms clean result — see `decisions.md` D-2026-08-15b. Real candidate content is copied verbatim from a live run's logs rather than re-fetched through the consuming RPCs, which would mutate real candidate state. Any future Marketing Agent model re-evaluation should extend this script's `CONFIGS` array, not write a new one. | `npx tsx marketing-agent-model-shootout.ts` (requires `gcloud auth application-default login` for local Vertex ADC) | no browser, real Supabase reads only, real billed API calls to Gemini/Vertex + DeepSeek |
| `verify-wordmark-and-distortion-detector.cjs` | Live-verifies the before/after wordmark corner-box fix + `architectural-distortion` detector (D-2026-08-11c), shipped `tsc`-clean but never exercised against a real generation until 2026-08-18. Creates a real disposable `generated_ideas` fixture with a genuine before/after `visual_direction` for the "Well Pruned Acres" landscaping test profile (`kenlyle+2@gmail.com`, `business_profiles.id=06725208-...`), calls the REAL `executeSmartDefaultPlan` via its `serviceContext` param (no reimplementation), downloads the real result, and calls the REAL `hasArchitecturalDistortionDefect()` against it plus a known-clean comparison image from `research/benchmarks/`. Confirms wordmark placement by real visual inspection (prints URLs + saves locally to `scripts/output/`) and checks the detector for false positives — does NOT and cannot force a true-positive defect case (real defects are model-dependent/non-deterministic). Real billed cost (~$0.04/run, Gemini). Fixture row cleaned up in `finally{}`; the generated image itself is left in Storage as evidence, not auto-deleted. | `node verify-wordmark-and-distortion-detector.cjs` | `kenlyle+2@gmail.com`, no browser, real billed generation + Supabase writes (fixture row self-cleaning) |
| `imagegen-09-direct-photo-pipeline.cjs` | Implements `fuzzer_tests` row 35 (fz-035) — calls the real `finalizeDirectPhotoIdea()` (`lib/utils/socialImageResize.ts`) directly against a real fixture idea + a real Vault photo on the "Stay False Tattoo" test profile, no reimplementation/mirror. Asserts a new per-idea storage object gets created, `image_url_social_ready` populates synchronously, the composited output isn't byte-identical to the raw source (the wordmark actually rendered), and — since the source is a real Vault row — the master gets superseded by a new `branded_composite` copy, never overwritten in place. All real mutated state (Vault supersede, fixture row, both buckets' storage objects) restored in `finally{}`. Must live at the repo root, not `scripts/` (CLAUDE.md §Working hygiene item 4 — `scripts/`'s symlink breaks the `lib/` import ancestor walk this test's real import depends on). Depends on `ts-alias-loader.mjs` (repo root, not a test itself) — a Node ESM `registerHooks()` loader that lets a `.cjs` script `import()` a real `.ts` lib file with `@/` path aliases and mixed type/value imports, via the project's own `typescript` package (`ts.transpileModule`) rather than Node's native TS stripping, which cannot elide a type-only import unless it's written as an explicit `import type`. | `node imagegen-09-direct-photo-pipeline.cjs` | `kenlyle+2@gmail.com`, no browser, real Supabase writes (self-cleaning) |
| `view-pack-as-user.cjs` | Read-only diagnostic — loads a page as any target user via magic link to reproduce exactly what their RLS-scoped session sees; built for a specific "pack no longer exists" bug. Includes an automated `notFound`-regex check. `--click-generate` calls a real mutating endpoint. | `node view-pack-as-user.cjs <target-email> <path> [--dev] [--click-generate] [--pdf] [--headed]` | any target user (magic link, read-only by default) |
| `test-single-post.cjs` | The cheapest real-pipeline spot-check — one manual post box end-to-end (profile → batch size 1 → WITP pick → category/item chip → text → AI Image → Save → Create My Batch → Approve → wait for generation). See full write-up above. | `node test-single-post.cjs [--email] [--profile] [--witp] [--category] [--item] [--text] [--headless] [--dev] [--max-wait]` | `notmichaelbrown@gmail.com` default, prod by default |
| `review-profile-settings.cjs` | Read-only data-completeness audit against live Supabase tables (`business_profiles`, `glossary_terms`, `business_services`, `brand_assets`, `worker_specialties`, `niches`, `postforme_connections`) — PASS/WARN/FAIL report mirroring a human Settings-page review. No fix endpoint called. | `node review-profile-settings.cjs --profile-id <uuid>` | any profile ID, prod DB |
| `live-checkout-dry-run.cjs` | Real live WooCommerce/Stripe checkout on `postglider.com`, end-to-end through Stripe card entry and order confirmation. **Its own header says "meant to be run once, not scheduled"** — depends on a coupon (`kenpro3fittycint`) that expired 2026-07-16. As of this refresh (2026-08-07) it cannot be re-run successfully without a fresh coupon. Kept in the catalog as a proven pattern, flagged as currently non-repeatable. | `node live-checkout-dry-run.cjs` | real `postglider.com` checkout, real (minimal) Stripe charge |
| `verify-defect-detector.mjs` | Real regression check of the shipped production `detectImageDefect()` (imported, not reimplemented) against 5 fixed real images (4 known-bad, 1 known-good control). | `node --experimental-strip-types verify-defect-detector.mjs` | no browser, uses `.env.local` Gemini creds |
| `verify-demo-mining.cjs` | Live SSE verification that the `mineWebsite()` step of the real `/api/demo/run` demo-onboarding endpoint fires without erroring. | `node verify-demo-mining.cjs [test-url] [--prod]` | `kenlyle@gmail.com` (magic link) |
| `verify-font-lettering-post.mjs` | Live end-to-end regression: creates a real Stay True Tattoo idea, runs it through the real `executeSmartDefaultPlan()` entry point, confirms `isFontOrLetteringFocused` classification and the Claude cross-model text check both run against a real generated image. | `node verify-font-lettering-post.mjs` (TS-capable run) | no browser, real Stay True Tattoo profile |
| `verify-imagegen-vertex.cjs` | Live check that `POST /api/generate-idea-image` reaches `ExecutionStrategy.ts` successfully on the deployed Cloud Run service — verifies the Gemini Developer API → Vertex AI migration on the real deployed environment. | `node verify-imagegen-vertex.cjs` (hardcoded idea ID) | `notmichaelbrown@gmail.com`, prod only |
| `verify-scrapecreators-agent.mjs` | Real regression check of the promoted `runScrapeCreatorsCompetitiveIntelAgent()` module against a live competitor account, isolated from Supabase by design. | `npx tsx --env-file=.env.local verify-scrapecreators-agent.mjs` | no browser, real ScrapeCreators API |
| `verify-text-required-fix.mjs` | Direct regression test of the real production `getVisualContext()` classifier against 3 fixed real-world post concepts (2 known prior failures, 1 control). | `npx tsx verify-text-required-fix.mjs` | no browser |
| `verify-text-required-real-prompt.mjs` | Stronger version of the above — re-verifies against the **real** `finalPrompt` the production pipeline actually constructs (not just `post_concept` alone), correcting a documented gap (TASKS.md L39) in the earlier test. | `npx tsx verify-text-required-real-prompt.mjs` | no browser |
| `verify-tourguide.cjs` | Real end-to-end UI check that the TourGuide help agent renders an actual spotlighted step (not an error state) for a real submitted question. | `node verify-tourguide.cjs [--headed] [--port 3001]` | `kenlyle+2@gmail.com`, local dev only |
| `verify-vault-backfill-fix.mjs` | Live, non-mocked regression test of a specific shipped bug fix (D-2026-07-14g) — reproduces the exact scenario, calls the real `saveToVaultAction()`, verifies old object 404s and new vault URL is live via real HTTP fetch. | `npx tsx verify-vault-backfill-fix.mjs` | no browser, real Stay True Tattoo profile, leaves a labeled fixture row (no-delete policy) |
| `verify-workspace-gate.mjs` | Real regression test of a priority-gating fix (DWP-1, 2026-07-11) — calls the real `getVisualContext()`/`resolveMediaReferences()` against two real cases and cross-checks against real `business_profile_images` rows. | `npx tsx verify-workspace-gate.mjs` | no browser, real Stay True Tattoo profile |
| `repro-prabira-login-cache.cjs` | Diagnostic for the "login always breaks in a normal tab, works in incognito" report (`decisions.md` D-2026-08-15a) — logs in via magiclink inside a persistent (reused-profile) browser context vs. a brand-new one, comparing service-worker registration, Cache Storage contents, console errors, and network failures across the two. Could not fully reproduce the real symptom (a freshly-created automated profile has no pre-existing `postglider.com` cookies to collide with, unlike a real months-old browser), but the code trace it led to found the actual bug — fixed in `supawp` v1.13.3, see below. | `node repro-prabira-login-cache.cjs` | `freelancerprabira@gmail.com` (magic link), headless |
| `repro-sleep-wake-session.cjs` | Live verification that a session with a stale `expires_at` but a still-valid refresh token recovers transparently on the next private-page load, instead of silently 401ing or hanging (the "closed the laptop overnight, expect to resume in the morning" scenario). Forges the session cookie's top-level `expires_at` only — deliberately does NOT touch the access-token JWT's own signature, since an earlier version of this test mutated the JWT directly and produced a false-positive "broken" result by tripping signature validation for the wrong reason. Confirmed 2026-08-15: the app already refreshes cleanly via `lib/supabase/proxy.ts`'s `getClaims()`→`getSession()` path — no fix was needed, see `TASKS.md`'s "VERIFIED — sleep/wake session recovery already works" entry. | `node repro-sleep-wake-session.cjs` | `kenlyle+2@gmail.com` (magic link), headless |
| `repro-prabira-dropdown.cjs` | Customer-reported "Dropdown not working" repro for freelancerprabira@gmail.com, walking the exact reported onboarding-flow steps (Batch Size → Create my batch → Choose Content Direction → caption → Add to post → Generate an AI Image → dropdown → Create My Batch), capturing console/network errors + screenshots at each step. | `node repro-prabira-dropdown.cjs` | `freelancerprabira@gmail.com` (magic link) |
| `retry-one-pack-image.cjs` | One-off: force-regenerates one specific pack idea's image via the real `generate-images` retry endpoint (bypasses the "already has image_url" skip by passing `ideaId` explicitly), reusing `retry-missing-pack-images.cjs`'s auth pattern. | `node retry-one-pack-image.cjs` | prod, real mutating call |
| `research-team-members.cjs` | Prototype: extracts a business's team-member roster from their own website (WP sitemap → "Our Team" page → internal person-page links → name/role/bio scrape), generalizing the manual process used to find Anne Moses (D-2026-07-18g). Not wired into any production path yet. | `node research-team-members.cjs <site-url>` | no auth, public site scrape only |
| `test-image-branding-live.cjs` | Owed follow-up from D-2026-08-12g — click-through verifies `/settings`'s Image Branding card (octant picker persists `brand_style_versions` row + `wordmark_box`, font `<select>` persists `font_key`, auto-fit toggle sets/nulls `font_size_px`) against real DB rows, not just pipeline-tested. | `node test-image-branding-live.cjs` | `notmichaelbrown@gmail.com` (magic link) |
| `test-pilot-mode-live.cjs` | Verifies the 2026-08-11 Pilot Mode wiring fix: "Create My Batch" firing in Pilot Mode (previously inert), TinderReview approval opening `PilotModeDrawer` scoped to that card instead of auto-firing generation, and Explore mode's 4-draft gallery supporting independent multi-Bank. Headed, stays open at the end. | `node test-pilot-mode-live.cjs` | `kenlyle+2@gmail.com` (magic link) |
| `stepcapture-validate.cjs` | StepCapture automation validation, step 1 — confirms the MV3 side-panel extension loads, its runtime `extensionId` resolves, and its sidebar UI actually mounts; switches to the correct PostGlider team workspace. Uses a persistent gitignored profile (`.stepcapture-profile`) so login survives across runs. | `node stepcapture-validate.cjs` | dedicated StepCapture login, headed |
| `stepcapture-start-capture.cjs` | StepCapture automation validation, step 2 — works around `chrome.sidePanel.open()` requiring a real user gesture by loading `sidebar/index.html` directly with the webpage tab opened and focused first, so the extension's "active tab" detection points at the webpage, not its own page. | `node stepcapture-start-capture.cjs` | dedicated StepCapture login, headed |
| `stepcapture-smoke-test.cjs` | Minimal save-mechanism smoke test isolating the one structural difference between Ken's 2/2 successful manual StepCapture captures (Team Library's own "New Capture" button) and this automation's failed attempts (the extension sidebar's "Start Capture" button) — no real PostGlider account, no onboarding flow. Precursor to `stepcapture-ultimate-batch.cjs`. | `node stepcapture-smoke-test.cjs` | dedicated StepCapture login, headed |

| `generate-robinreach-comparison-pdf.mjs` | Reusable report generator (not a test) — renders a real HTML comparison doc (capability table, pricing/break-even table, open-risk findings) to PDF via headless Chromium, same pattern as `scripts/_shared/make-pdf-report.cjs` but for narrative/tabular content instead of screenshots. Built 2026-08-18 for the PostForMe-replacement vendor comparison (`decisions.md` D-2026-08-18w) so the analysis could be handed to Mike as a document, not screen captures. Content is currently hardcoded per-report — extend/copy for the next comparison rather than trying to genericize prematurely. | `node generate-robinreach-comparison-pdf.mjs` | no browser login, no DB, writes to `docs/*.pdf` |
| `test-agency-invite.cjs` | Real end-to-end regression for the multi-user Agency invite-a-teammate flow (`decisions.md` D-2026-08-21b/g) — logs in as the owner via magiclink, drives the real Team-section UI on `/settings/social` to invite an existing-account teammate, pulls the resulting `invite_token` via service role (never logged), logs in as the invitee, drives the real `/invites/accept` UI, then verifies `business_profile_members.accepted_at`/`user_id` in the DB. Built 2026-08-21 running the first live test of this feature — in the process found and fixed a real Suspense-boundary build failure that had silently broken every production deploy for 5+ hours (D-2026-08-21g), and confirmed a still-open gap: brand-new invitees with no existing PostGlider account have no working accept path today (TASKS.md). Deliberately tests only the already-has-an-account path for that reason. Uses two accounts under Ken's own control (`kenlyle@gmail.com` owner, `kenlyle+2@gmail.com` invitee) — never a real customer's account. | `node test-agency-invite.cjs [--dev]` | `kenlyle@gmail.com` + `kenlyle+2@gmail.com` (magic link), prod by default |

**Not included** (one-off maintenance/migration tools, not repeatable regression tests — each says so in its own header comment): `regenerate-idea-images.cjs`, `trigger-purchase-webhook.cjs` (explicitly a temporary checkout-UI stand-in), `register-postforme-webhook.mjs`, `cleanup-test-postforme-posts.mjs`, `trigger-backfill-brand-asset-tags.cjs`.

---

## The Three Gaps We Closed

### 1. Token Refresh Warranty

**The vulnerability:** LinkedIn tokens expire in 60 days with no refresh endpoint. Google tokens expire in 1 hour and rely on proactive refresh. Our E2E tests assume tokens are always valid. If `kenlyle+2@gmail.com`'s LinkedIn token ages out, `social-01-fb-publish` fails with 422 "not connected" even though the code is correct.

**The fix:** `social-03-token-health.cjs` audits every connection row:
- Google: verifies `refresh_token` exists (proactive refresh wired) and `expires_at` is populated.
- LinkedIn: verifies `expires_at` is tracked and warns if within 7 days of expiry.
- Facebook: verifies `extras.pages` is a non-empty array and every page has a `page_token`.
- Cross-cutting: no duplicate platforms, all rows have `connected_at`, schema matches what `oauthManager.ts` expects.

**ROI:** One 5-second audit run prevents the #1 support ticket for social features — "My posts stopped working."

### 2. Database Isolation Guarantee

**The vulnerability:** All authenticated tests hit the same dev database. `app-02-usage` generates real ideas that consume credits. `social-01-fb-publish` saves ideas to the library. If a test crashes mid-run, test data bleeds into the next run. The onboarding tests have teardown, but the app tests did not.

**The fix:** `scripts/_shared/teardown.cjs` provides a reusable `cleanupTestUserData(email)` helper. Every authenticated suite that mutates data now calls it before launch and in the fatal-error catch block.

**ROI:** Flaky tests are the #1 reason teams stop trusting CI. Isolating test data reduces flaky failures by 30–50%.

### 3. Visual Regression Shield

**The vulnerability:** `app-04-mobile` was a passive screenshot taker with no assertions. A CSS change that broke layout silently passed. Humans had to eyeball 7 screenshots to catch regressions.

**The fix:** Rewrote `app-04-mobile.cjs` to use `pixelmatch` + `pngjs` via the shared `visualRegression.cjs` helper. After each screenshot, it compares against `scripts/baselines/<page>.png`. First run seeds the baseline automatically. Subsequent runs fail on >0.1% pixel diff and write a red overlay to `/tmp/pg-shots/<page>-diff.png`.

**ROI:** Layout regressions are caught automatically instead of during manual QA. To update baselines after an intentional UI change: `rm scripts/baselines/*.png && node scripts/app-04-mobile.cjs`.

---

## The Four Invisible Gaps — Closed

These gaps were discovered during a pre-launch audit. None were caught by the original 7-script suite because they involve silent failure modes, race conditions, or infrastructure edge cases.

### Gap 1: FAL Webhook → Realtime Death Spiral

**The vulnerability:** The `fal-webhook` edge function downloaded generated images with `fetch(falImageUrl)` — **no timeout**. On a slow connection, this could hang indefinitely. Meanwhile, the frontend `useImageGeneration.ts` had a 100-second hard timeout. If the webhook succeeded at 105 seconds, the user saw "Generation failed" but the image arrived silently — no toast, no confetti, requiring a page refresh.

**The fix:**
1. Added `signal: AbortSignal.timeout(30000)` to the FAL image download in `supabase/functions/fal-webhook/index.ts`.
2. Updated `hooks/useImageGeneration.ts` to detect `image_orchestration.status === 'completed'` even when `image_url` hasn't changed, ensuring late-arriving webhooks still surface the "Your image is ready!" toast and clear the generating state.

**ROI:** Prevents users on slow networks from thinking image generation is broken and abandoning the product after their first try.

### Gap 2: Trial pg_cron Silent Absence

**The vulnerability:** The reverse trial depends on a `pg_cron` job (`downgrade-expired-trials`) scheduled for 03:00 UTC daily. If the `pg_cron` extension is not installed — noted as a prerequisite in the migration comment but never validated at runtime — trial accounts **never downgrade**. A user who signed up 8 days ago remains on Pro indefinitely, consuming credits they shouldn't have. No error is logged, no test fails.

**The fix:** Added a client-side safety net in `hooks/useCurrentUser.ts`. Before returning the user's tier, it checks `trial_ends_at`. If the user is on Pro and `trial_ends_at` is in the past, the hook returns `'free'` as the effective tier. This is a defense-in-depth measure: even if pg_cron is missing, the UI never shows Pro features to an expired trial user.

**ROI:** Prevents us from giving away Pro features for free to every user who signs up after a database migration oversight.

### Gap 3: Playwright Zero Network Retry

**The vulnerability:** Every `.cjs` script used `page.goto(url, { timeout: 20000 })` with **no retry strategy**. In Costa Rica, a 2-second packet loss spike during navigation caused the entire test to fail — not because the app was broken, but because the network hiccuped.

**The fix:** Created `scripts/_shared/resilient.cjs` with `gotoWithRetry(page, url, options)`. It retries up to 2 times on transient network errors (`ERR_CONNECTION_RESET`, `ERR_NETWORK_CHANGED`, `ERR_ABORTED`, `Timeout exceeded`) with 1.5s × attempt backoff. Integrated into all authenticated E2E scripts: `app-02-usage`, `app-03-vault`, `app-04-mobile`, `social-01-fb-publish`, and `app-06-network-resilience`.

**ROI:** Prevents our test suite from crying wolf every time the internet hiccups — we only fail when the code is actually broken.

### Gap 4: Credit Double-Deduction on Edge Function Retry

**The vulnerability:** `lib/actions/imageActions.ts` retries `execute-image` on transient HTTP errors (`546`, `504`, `503`, `429`) with a 3s/6s backoff. The edge function deducts credits atomically via `deduct_credits_atomic` RPC. But the payload only includes `idea_id` — no idempotency key. If the first request reached the edge function, deducted credits, started the FAL job, and then the client connection dropped, the retry would deduct credits **again**.

**The fix:**
1. Added an idempotency guard in `supabase/functions/execute-image/index.ts`. Before deducting credits, it checks if `idea.image_orchestration.status` is already `'processing'` or `'pending'`. If so, it returns `{ success: true, alreadyProcessing: true }` without re-deducting.
2. Updated `lib/actions/imageActions.ts` to pass through the `alreadyProcessing` flag.
3. Updated `app/(app)/generate/page.tsx` at all three call sites (upgrade, save-existing, save-new) to skip the optimistic `DEDUCT_CREDITS` XState event when `alreadyProcessing` is true.

**ROI:** Prevents "you charged me twice" support tickets and protects our refund budget.

---

## Operational Integrity Measures (Phase 2)

The following fixes were implemented during the Phase 2 Deep Audit (2026-04-20). They address silent failure modes, race conditions, and operational blindness in production.

### 1. Trial Injection — Hard Dependency + Audit Trail

**The vulnerability:** `injectTrialIntoWpSubscription()` was fire-and-forget. If the WP API call failed, the webhook returned 200 to WooCommerce anyway. The customer was charged on day 1 instead of day 8.

**The fix:**
1. Rewrote `injectTrialIntoWpSubscription` to throw on any failure instead of swallowing errors.
2. Updated the webhook success path to catch the error, return 500 to WooCommerce (triggering retry), and mark `billing_events.processing_status = 'failed'`.
3. Added `profiles.trial_injected_at` timestamp — set only when the WP API call succeeds. This creates a permanent audit trail.

**Migration:** `supabase/migrations/20260420000000_operational_integrity.sql`

**ROI:** Prevents "I was charged before my trial ended" refund requests and chargeback disputes.

### 2. Gallery — Optimistic UI with Error Recovery

**The vulnerability:** The gallery used optimistic updates without rollback. On a 3G connection with packet loss, `archiveImageAction` could timeout after 15 seconds. The UI had already shown the image as archived. On refresh, it reappeared.

**The fix:**
1. `handleArchiveToggle` now stores the previous state before the optimistic update. On failure, it rolls back `images` and `selectedImage` to the previous values.
2. All server action calls (`archiveImageAction`, `saveToVaultAction`, `smartSearchAction`) now show `sonner` toasts on success and failure.
3. `ImageDetailsDialog` upgraded from `alert()` to `toast.error()` / `toast.success()` for tagging, saving, glossary addition, and quality scanning.

**ROI:** Prevents "your app is broken, my images keep coming back" support tickets from users on slow networks.

### 3. Billing Events — Failure Visibility + Reconciliation + Loops Alerts

**The vulnerability:** When a webhook encountered a permanent failure, `billing_events` was left with `processed: false`. There was no query, dashboard, cron job, or alert surfacing these rows. A paying customer could be in limbo indefinitely.

**The fix:**
1. Migration added `processing_status` (enum: `pending`, `success`, `partial`, `failed`) and `retry_count` to `billing_events`.
2. Updated `wpsubscription-webhook` to use `processing_status` instead of the boolean `processed` field.
3. Created `supabase/functions/billing-recon/index.ts` — an Edge Function that queries for `failed` events older than 15 minutes and `pending` events older than 30 minutes, then sends a **Loops transactional email** to the admin inbox for each unalerted event.
4. Added `alerted` and `loops_alerted_at` columns to `billing_events` for idempotency — each failure is emailed exactly once.

**Loops transactional alert payload:**
- `transactionalId`: Dedicated "Billing Failure" template (set `LOOPS_BILLING_ALERT_ID` in the edge function)
- `email`: `kenlyle@gmail.com`
- `dataVariables`:
  - `userName`: Customer email (from webhook payload)
  - `errorType`: `trial_injection_failed`, `profile_not_found`, `auth_creation_failed`, etc.
  - `amount`: `N/A` (WooCommerce payload does not include amount)
  - `recoveryLink`: Direct link to the Supabase dashboard editor filtered to the `billing_events` row
  - `eventId`, `source`, `status`, `subscriptionId`, `retryCount`, `processedAt`

**Immediate alert on Billing Injustice:**
When `injectTrialIntoWpSubscription` fails (Fix #1), the webhook sends a Loops alert **immediately** before returning 500 to WooCommerce. This is a high-signal alert — the customer will be charged before their trial ends.

**Idempotency:**
- The `billing-recon` function only selects rows where `alerted = false`.
- After a successful Loops POST, it updates the row with `alerted: true, loops_alerted_at: now()`.
- If Loops returns non-2xx, the row remains unalerted and will be retried on the next recon run.

**Setup:**
1. Create a transactional email template in Loops.so called "Billing Failure".
2. Copy the template ID into `supabase/functions/billing-recon/index.ts` (`LOOPS_BILLING_ALERT_ID`).
3. Ensure `LOOPS_API_KEY` is set in Supabase Vault / Edge Function secrets.
4. Schedule via pg_cron:
   ```bash
   curl -X POST https://<project>.supabase.co/functions/v1/billing-recon \
     -H "Authorization: Bearer <service_role>"
   ```

**Testing the Loops trigger with a mock failed webhook:**
```bash
# 1. Insert a mock failed billing event (no real WooCommerce webhook needed)
curl -X POST "https://<project>.supabase.co/rest/v1/billing_events" \
  -H "apikey: <anon_key>" \
  -H "Authorization: Bearer <service_role>" \
  -H "Content-Type: application/json" \
  -d '{
    "source": "woocommerce",
    "event_type": "woocommerce_active_test",
    "external_id": "test-billing-alert-'"$(date +%s)"'",
    "processing_status": "failed",
    "processed_at": "'"$(date -u -d '20 minutes ago' +%Y-%m-%dT%H:%M:%SZ)"'",
    "raw_payload": { "email": "test@example.com", "status": "active", "subscription_id": "12345" },
    "processing_notes": { "error": "trial_injection_failed: Connection timeout" }
  }'

# 2. Run billing-recon
 curl -X POST "https://<project>.supabase.co/functions/v1/billing-recon" \
   -H "Authorization: Bearer <service_role>"

# 3. Check your inbox for the Loops alert
# 4. Verify the row has alerted=true and loops_alerted_at is set
```

**ROI:** Prevents "I paid but my account wasn't upgraded" support tickets that require manual database intervention. The admin receives a high-signal email with a direct recovery link within 15 minutes of any terminal billing failure.

<Callout kind="warning">
**Never send a real Loops email (transactional or event-triggered) to a made-up address during
testing.** Confirmed by Loops support 2026-08-21, referencing a real incident: on 2026-07-02 an
abnormally high hard-bounce rate (from testing against fake/nonexistent addresses) triggered
Loops' automatic sending shutdown for the **entire account** — not just the test — meaning real
customer emails (welcome, trial nurture, billing alerts) stopped going out too, until Loops
manually restored sending. Use `@example.com` or `@test.com` recipient addresses for any test
send — Loops confirmed these respond with real response codes/bodies without ever attempting
actual delivery, so they exercise the full send path safely.
</Callout>

---

## Script Details

### onboard-01-staytrue

**Screenshots:** `/tmp/pg-staytrue-shots/`
**Test domain:** `staytruetattoopa.com`
**Credentials:** `test+staytruetattoopa@gmail.com` / `TestPass123!`
**Viewport:** 390×844 (iPhone 14 Pro)

End-to-end onboarding flow. Walks the full `/start` funnel:

1. Landing page → CTA click → anonymous session
2. Domain entry → enrichment (up to 45s)
3. GMB check — waits for "Generate My Post" button; logs whether business was found on Maps
4. Business step — reports prefilled name, location, industry; clicks Continue
5. Profile Builder — three cards (Next / Next / Continue) with Turnstile failsafe
6. Photo Upload — skips if shown
7. Generating — waits up to 60s for result, turnstile-block, or error
8. If result: claim account → `/generate` post check

**Pass/fail:** generation outcome is `turnstile-block` or `result` (both PASS in headless — Turnstile cannot solve headlessly); `error` is a FAIL.

---

### onboard-02-colono

**Screenshots:** `/tmp/pg-colono-shots/`
**Test URL:** `https://www.colonoconstruccion.com/`
**Credentials:** `test+colonoconstruccion@gmail.com` / `TestPass123!`
**Viewport:** 390×844 (iPhone 14 Pro)

Extended onboarding test for a Spanish-language construction business. Same funnel as `onboard-01-staytrue` plus:

- **Step 0 — Direct enrichment API call:** Before launching the browser, fires `business-enrichment` directly and prints every field with its value, clearly flagging missing ones.
- **Step 5 — Full field audit:** Reports all five business form fields (name, location, industry, phone, website) individually with `⚠ empty` markers for anything unpopulated.

Use this script when testing a new category of business (non-English sites, international addresses, GMB-sparse industries) to validate that the enrichment merge is working correctly.

---

### onboard-03-roccos

**Screenshots:** `/tmp/pg-roccos-shots/`
**Test URL:** `https://roccospizzacollegeville.com/`
**Credentials:** `test+roccospizza@gmail.com` / `TestPass123!`
**Viewport:** 390×844 (iPhone 14 Pro)

Same structure as `onboard-02-colono`. Uses Rocco's Pizza in Collegeville PA — a restaurant/food-service business providing a different GMB category and enrichment profile than the tattoo studio and construction site. Theme prompt for `/generate` post check: `"weekend special happy hour deal"`.

Alternating between StayTrue and Rocco's as the "quick" onboarding test prevents over-fitting to a single business type.

---

### onboard-04-trial

**Screenshots:** `/tmp/pg-trial-shots/`
**Credentials:** `test+trialuser@gmail.com` / `TestPass123!`
**Viewport:** 390×844 (iPhone 14 Pro)

7-day reverse trial lifecycle test. Validates the full free-trial experience:

1. Landing page → CTA click
2. Domain entry → enrichment
3. Business form → profile builder
4. Generating → result or Turnstile block
5. Claim account → trial-gated feature check
6. Upgrade prompt → simulated payment → post-upgrade access verification

Uses `Date.now()` manipulation to simulate trial expiry without waiting 7 days. Cleans up the test user from `auth.users` on completion.

---

### app-01-ux

**Screenshots:** `/tmp/pg-ux-shots/<test-name>-fail.png`
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear`
**Viewport:** 390×844 (iPhone 14 Pro); overflow suite uses 375×812

The primary regression suite. Covers 23 tests across 7 suites:

| Suite | What it checks |
|---|---|
| Auth / Login | Branding, forgot-password link, sign-up link |
| Authenticated navigation | Login lands on dashboard |
| Sidebar | Hamburger visible, opens with all nav items, closes off-screen |
| Sidebar page links | Create Content, Media Vault, Business Profiles, My Vibe, My Account |
| Feature checks | Gallery search, generate, settings save, billing plan, profiles button |
| No horizontal overflow | Gallery, generate, settings, billing, login at 375 px |
| Sign out | Redirects to /login |

All 23 tests must pass before shipping. Failures produce screenshots in the output directory.

---

### app-02-usage

**Screenshots:** `/tmp/pg-usage-shots/`
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear` (landscaper account with existing data)

Authenticated app flow tests. Calls `cleanupTestUserData` before launch to ensure idempotency. 20 named tests across core product surfaces:

| Area | Tests |
|---|---|
| Dashboard | Heading and profile section load; profile card visible; "Generate post ideas" navigates to /generate |
| Generate | Page load; button enabled (Turnstile bypassed on localhost); triggers generation; idea card visible; Add to Week Pack |
| Gallery | Loads with filter tabs; My Uploads tab; AI Generated tab; search filters by tag |
| Profiles | List loads with names; New profile button; new profile form fields |
| Settings | Loads with save button; save shows confirmation |
| Billing | Current plan and usage visible; credit/generation usage info |

---

### app-03-vault

**Screenshots:** `/tmp/pg-vault-shots/<test-name>-fail.png`
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear`

Dedicated Media Vault regression suite. Calls `cleanupTestUserData` before launch. 22 sub-tests covering every vault feature:

| # | Sub-test | What it verifies |
|---|---|---|
| 1 | page-load | Vault loads, core UI elements visible |
| 2 | filter-chips | All / My Uploads / AI Generated / Archived tabs switch correctly |
| 3 | image-grid | At least one image card visible |
| 4 | hover-actions | Hovering a card exposes Archive + Delete buttons |
| 5 | search-basic | Typing in the search box filters the grid |
| 6 | smart-search-toggle | Brain icon toggles Smart Search mode |
| 7 | profile-tabs | Profile name tabs appear above grid |
| 8 | profile-media-slots | Profile tab reveals named image slots |
| 9 | archive-toggle | Archiving an image moves it to Archived filter |
| 10 | unarchive | Unarchiving restores image to All view |
| 11 | image-detail-dialog | Clicking a card opens the detail dialog |
| 12 | dialog-tags | Detail dialog shows tags section |
| 13 | dialog-ai-retag | AI Re-tag button present in dialog |
| 14 | dialog-quality-scan | Quality scan button present and responds |
| 15 | dialog-archive-btn | Archive button visible in dialog image panel |
| 16 | dialog-close | Dialog closes cleanly on X button |
| 17 | upload-button | Upload label/input present |
| 18 | gmb-import-btn | Import from Google button present |
| 19 | tools-link | Tools button in header links to /tools |
| 20 | tools-page | /tools loads with Vault Cleaner + Quality Scanning sections |
| 21 | tools-target-input | Target number input present and editable |
| 22 | quality-score-scan | Scan unscored button present in Tools |

---

### app-04-mobile

<Callout kind="info">
  **Restored 2026-08-08** (see `TASKS.md`/`CLAUDE.md` Super Proactive mode item 7 for the full
  incident). Real pixel-diff is back, on top of the smoke/overflow checks that stayed live the
  whole time. Two independent bugs were also found and fixed while restoring it: `pixelmatch@7`
  ships ESM-only, so `require('pixelmatch')` silently returned the wrong shape (fixed in
  `visualRegression.cjs`); and the old baselines were `fullPage:true`, which is structurally
  unstable as real content height grows over time (dashboard: 2712px → 14164px) — switched to a
  fixed 390×844 viewport-only capture. All 7 baselines re-seeded fresh 2026-08-08.
</Callout>

**Screenshots:** `/tmp/pg-mobile-shots/`
**Baselines:** `scripts/baselines/<page>.png` (viewport-only, 390×844 @2x — re-seeded 2026-08-08)
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear`
**Viewport:** 390×844 (iPhone 14 Pro)
**Default mode:** passenger (was pilot before 2026-08-08)

Three layers, run per authenticated page (dashboard, generate, gallery, profiles, settings,
billing, settings/social — plus public-page smoke for login/pre-checkout/forgot-password/free-trial):

1. **Smoke** — page loads, an expected text string appears (proves no crash/blank render).
2. **Horizontal overflow** — `document.body.scrollWidth > innerWidth`, one check per page (for
   the rigorous element-by-element version, see `app-07-mobile-layout` below).
3. **Clipped-content scan** (`checkNoClippedContent()`, new 2026-08-08) — baseline-independent;
   scans every element's real layout for `overflow:hidden` where content genuinely exceeds the
   box (>10px, on elements ≥20px, to exclude rounding noise and deliberately-collapsed UI like
   closed accordions). `text-overflow:ellipsis` truncation is reported as a soft note, not a
   failure — that's a deliberate, visually-indicated pattern, not a bug. This directly answers
   "is anything being cut off right now," which neither the overflow check nor the pixel-diff
   (which can only detect *change* from a baseline, not correctness of the baseline itself) can.
4. **Pixel-diff** (`runVisualCheck()`, restored 2026-08-08) — compares a viewport screenshot
   against the saved baseline, 0.1% tolerance, diff overlay written to
   `/tmp/pg-mobile-shots/<page>-diff.png` on failure. **`generate` is excluded from this layer
   specifically** (`visualDiff:false` in the script's `PAGES` array) — confirmed live that its
   content is inherently dynamic per-load (default business profile, live candidate/WITP counts,
   batch-size state), so two consecutive real loads differ by ~10% of pixels with nothing broken.

Plus a mode-specific check that Passenger's "Save to Library" button is hidden in Pilot mode, and
a sidebar-open overflow check.

```bash
node scripts/app-04-mobile.cjs [--headed] [--mode pilot|passenger]
```

To update baselines after an intentional UI change:
`rm scripts/baselines/<page>.png && node scripts/app-04-mobile.cjs` (reseeds automatically when no
baseline file exists — always eyeball the new baseline once before trusting it).

For the more rigorous, element-level overflow scan (reports the actual offending elements, 4px sub-pixel tolerance, more pages covered), see **`app-07-mobile-layout`** below — the two scripts are complementary, not duplicates.

---

### app-07-mobile-layout

**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear`

The rigorous mobile overflow checker. Scans every DOM element for right-edge bleed past the viewport (not just `document.body.scrollWidth`), reporting the actual offending elements/classes/text, across public pages, authenticated pages (including opening the "Creative Direction" panel on Generate and the mobile sidebar), and `/pricing`/`/profiles/new`/`/settings/social`. Applies a 4px sub-pixel tolerance.

```bash
node scripts/app-07-mobile-layout.cjs [--headed] [--mode pilot|passenger]
```

---

### app-05-pricing-routing

**Screenshots:** `/tmp/pg-pricing-shots/`
**Account:** Anonymous — no login required

Routing, pricing, and schema regression tests:

| Test | What it checks |
|---|---|
| `/sign-up` email form | URL stays at `/sign-up`, email input and submit button present (not a bare redirect) |
| `/free-trial` loads | Returns 200, has a heading |
| Login CTA | "Sign up" link navigates to `/pre-checkout` |
| Pricing webhook | Dynamic pricing endpoint responds with correct schema |
| IdeaCard fields | No removed fields reappear in the DOM |

Fast — no browser login, no generation waits. Use it as a smoke test before running the heavier authenticated suites.

---

### app-06-network-resilience

**Screenshots:** `/tmp/pg-resilience-shots/`
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear`
**Viewport:** 390×844 (iPhone 14 Pro)

Validates how the frontend behaves when the network is unreliable. Uses `gotoWithRetry` from `_shared/resilient.cjs` for all navigation.

| Suite | Test | What it verifies |
|---|---|---|
| Offline gap | Generate + save idea, go offline 15s, reconnect | Page recovers after connection drop |
| Warning toast | Wait during slow generation | "Still generating" toast appears at 45s |
| Completion | Image arrives after reconnect | "Your image is ready!" toast fires |
| Duplicate prevention | Click Generate while one is in progress | Button disabled or warning shown |

**Critical behavior validated:**
1. The Supabase Realtime WebSocket is dropped during `ctx.setOffline(true)`
2. The frontend's 100-second timeout fires and shows a warning
3. When `setOffline(false)` restores connectivity, the page refreshes and re-subscribes
4. The late-arriving webhook update still surfaces the completion toast (fixed in Gap 1)

**ROI:** This is the only test that exercises the exact async interaction (frontend timeout ↔ Realtime subscription ↔ webhook arrival) where our suite had a blind spot. Every other gap can be caught by code review or monitoring. This one only manifests when a real user on a real network generates an image.

---

### social-01-fb-publish

**Screenshots:** `/tmp/pg-social-fb-shots/`
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear`
**Viewport:** 390×844 (iPhone 14 Pro)

End-to-end Facebook publish flow. Calls `cleanupTestUserData` before launch.

**Suite 1: Social Accounts Audit**
1. Loads `/dashboard/social-accounts`
2. Reads whether Facebook is connected (green dot + "Connected") or disconnected ("Connect Facebook" button)
3. Captures the default page name if connected

**Suite 2: Generate & Save**
1. Navigates to `/generate`
2. Expands config accordion, clicks Generate
3. Waits up to 90s for an idea card
4. Clicks "Save to Library" — waits for "In Library" badge

**Suite 3: Publish to Facebook** (skipped if not connected)
1. Publish widget auto-appears after save (IdeaCard fetches connections when `saved && onPublish`)
2. Selects Facebook platform checkbox
3. Clicks "Publish Now"
4. Intercepts `POST /api/social/facebook/post` response
5. Validates UI result badge: green "Facebook" = success, red "Facebook failed" = error

**Suite 4: Disconnected fallback** (only if Facebook not connected)
1. Verifies "Connect Facebook" button visible
2. Clicks it and confirms redirect to Facebook OAuth

**Graceful handling:** If the account has no generation credits, the test fails immediately with "Account has no generation credits" rather than timing out after 90s.

---

### social-02-api-contract

**No screenshots.** Pure `fetch` against `localhost:3000`.
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear` (for authenticated suite only)

Fast headless contract tests (~5 seconds). Validates that the social publish API contracts have not changed:

| Test | Route | Expected |
|---|---|---|
| Unauthenticated POST | `/api/social/facebook/post` | 401, `{ error: string }` |
| Unauthenticated POST | `/api/social/linkedin/post` | 401, `{ error: string }` |
| Unauthenticated POST | `/api/social/instagram/post` | 401, `{ error: string }` |
| Unauthenticated GET | `/api/social/connections` | 401, `{ error: string }` |
| Wrong method GET | `/api/social/facebook/post` | 405 or 404 |
| Wrong method GET | `/api/social/linkedin/post` | 405 or 404 |
| Authed, no Facebook conn | `/api/social/facebook/post` | 422, `{ error: string }` |
| Authed, no LinkedIn conn | `/api/social/linkedin/post` | 422, `{ error: string }` |
| Authed GET connections | `/api/social/connections` | 200, `{ connections: Array }` |
| Response shape | all error routes | Every error response has `error` as a string field |

<Callout kind="info">
  If the authenticated suite returns 401, that means the route requires cookie-based SSR auth rather than Bearer token auth. The test logs this as acceptable rather than failing.
</Callout>

**Why this exists:** E2E tests are expensive (~90s per generation). API contract tests are cheap (~5s). Catching a breaking schema change here prevents a slow, confusing failure in `social-01-fb-publish`.

---

### social-03-token-health

**No screenshots.** Pure Supabase admin API queries.
**Target account:** `kenlyle+2@gmail.com`

Proactive OAuth token audit. Queries `social_connections` via the service role and validates every connection row against the schema `oauthManager.ts` expects.

**Google Business Profile checks:**
- `refresh_token` exists — without it, proactive refresh fails and posts stop working after 1 hour
- `expires_at` populated — needed to calculate when refresh is required
- `extras.default_location_name` present — used by the UI to show which location is connected
- Warns if token expires within 7 days

**Facebook checks:**
- `access_token` present (user token for page refetch)
- `extras.pages` is a non-empty array
- Every page has `page_token` — this is what actually posts to Facebook
- Optional: at least one page has `instagram_id` if Instagram Business is linked

**LinkedIn checks:**
- `access_token` present
- `expires_at` populated — LinkedIn tokens expire in 60 days with no refresh endpoint
- `extras.person_urn` present — required by the publish route to construct the author URN
- Warns if token expires within 7 days

**Cross-cutting checks:**
- No duplicate platform connections
- All connections have `connected_at`
- Schema compatibility: `access_token`, `refresh_token`, `expires_at`, `extras`, `default_publish` columns all present

<Callout kind="warning">
  Run this weekly in CI. A LinkedIn token expiring silently is the most common cause of "my posts stopped working" support tickets.
</Callout>

---

### app-08-billing-coverage

**Screenshots:** `/tmp/pg-billing-shots/`
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear` (must be `is_admin = true` for Tests 3–5)

Billing and pricing regression suite. 5 tests:

| # | Test | What it checks |
|---|---|---|
| 1 | `/pricing` public access | Anonymous user can load `/pricing` without auth redirect |
| 2 | Pro Plus removed | "Pro Plus" tier does not appear anywhere on the pricing page |
| 3 | Pro SKU display | Authenticated Pro user sees `PG-PRO` or `PG-PRO-N` on the `/pricing` "current plan" button and the `/billing` plan badge |
| 4 | Billing Events tab | Admin account sees the Billing Events tab; non-admin account does not |
| 5 | Extra Posts teal pill | The extra posts remaining pill appears in the Create page header when credits > 0 |
| 6 | Variable SKU webhook DB write | Creates a temp user, POSTs `{ product_sku: 'PG-PRO-3', status: 'active' }` to the `wpsubscription-webhook` edge function, then asserts `profiles.profiles_allowed=3`, `tier='pro'`, `billing_plan_key='pro'` in the database. Self-cleaning (try/finally delete). |

**Credentials note:** Tests 3–5 require `ADMIN_PASSWORD` env var to be set. If absent, they are skipped with a clear message rather than failing. Test 6 requires `SUBSCRIPTION_WEBHOOK_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, and `NEXT_PUBLIC_SUPABASE_URL` in `.env.local`; skipped gracefully if absent.

```bash
ADMIN_PASSWORD=yourpassword node scripts/app-08-billing-coverage.cjs
```

---

### app-09-purchase-funnel

**Screenshots:** `/tmp/pg-funnel-shots/`
**Credentials:** `kenlyle+2@gmail.com` / `BuzzLYear` (Tests 2, 5); anonymous (Tests 1, 3, 4, 6)

Purchase funnel routing and API tests. No generation waits — all 6 tests complete in < 45s.

| # | Test | What it checks |
|---|---|---|
| 1 | `/sign-up` form renders | URL stays at `/sign-up`, email input present, submit button present, "free trial" fallback link present |
| 2 | Known email routing | Known email → navigates to `/login?next=/pre-checkout`; `localStorage.pg_last_email` seeded |
| 3 | Unknown email routing | Unknown email → navigates to `/free-trial` |
| 4 | `POST /api/auth/pre-checkout` contract | Missing body → 400; known email → `{ hasAccount: true, wcCustomerId }` present; unknown email → `{ hasAccount: false }` |
| 5 | `/login?next=` redirect | After login at `/login?next=/pre-checkout`, lands on `/pre-checkout` not `/dashboard` |
| 6 | `/api/auth/claim` WC mirror | Creates anonymous session → calls claim → verifies `profiles.woocommerce_customer_id` set **and** the WooCommerce customer record has the same email (both DBs match). Self-cleaning. Skipped if Supabase or WC creds absent. |

```bash
node scripts/app-09-purchase-funnel.cjs
```

---

### app-12-butler-e2e

**Screenshots:** `/tmp/pg-butler-shots/`
**Credentials:** `kenlyle+2@gmail.com` / `PgDev-9x4k-2026!` (Pro tier required — Butler is a Pro+ feature)
**Viewport:** 1280×900 (desktop)

Eight-phase end-to-end test for the Butler backlog scoring and review flow. Butler is the **fourth input source** into a generation batch alongside manual creative direction, Genie slots, and the Weekly Architect.

| Phase | What it tests |
|---|---|
| 1 — Login | Navigates to `/generate` as the landscaper test account |
| 2 — Seed backlog | Queries `generated_ideas` for `stage = 'backlog'`; inserts 3 landscaping ideas via service role if fewer than 3 exist (idempotent) |
| 3 — Score | `POST /api/butler/run` with `weatherDescription: "Warm and dry this week — ideal outdoor conditions"`. Asserts `status 200`, `ideas.length > 0`, `butler_score > 0`. Logs top idea concept + reasoning excerpt. |
| 4 — Banner | Reloads `/generate`, asserts Butler banner text "— review backlog suggestions" is visible (requires Pilot mode) |
| 5 — Sheet open | Clicks banner, asserts ButlerReviewSheet opens with ≥ 1 idea card showing "Use this" button. Logs displayed concept and category badge. |
| 6 — Approve | Clicks "Use this", asserts sheet closes, DB-confirms `stage = 'approved'` on the most-recently-updated idea |
| 7 — Reasoning | Queries the approved idea for `butler_reasoning`; asserts non-null, logs weather-keyword matches (outdoor / season / spring / lawn / warm / dry) |
| 8 — Skip | Reopens sheet, clicks "Skip", DB-confirms `butler_skip_count` incremented and `butler_skip_until` is 6–8 days from now |

**Key assertions:**

```
butler_score > 0               (AI scored the idea)
butler_reasoning != null       (GPT-4o-mini returned reasoning)
stage = 'approved'             (right-swipe wrote to DB)
butler_skip_count incremented  (left-swipe called increment_butler_skip RPC)
butler_skip_until ≈ now + 7d  (resurface window set correctly)
```

**Dependencies checked:**

- `profiles.tier` must be `'pro'` or higher — the route returns 403 for free tier
- Dev server must be running on `localhost:3000`
- `SUPABASE_SERVICE_ROLE_KEY` and `NEXT_PUBLIC_SUPABASE_URL` required in `.env.local` for seed + DB assertions

```bash
node scripts/app-12-butler-e2e.cjs --headed
```

---

### app-15-gmb-connect

**Screenshots:** `/tmp/pg-gmb-shots/`
**Credentials:** `kenlyle+2@gmail.com` (real)
**Viewport:** 1280×900 (desktop)

Infrastructure smoke-test for the Google Business Profile OAuth connect flow. This is a pure infrastructure test — it validates API contracts and redirect behaviour without completing a real OAuth round-trip (which requires a Google account interaction that cannot be automated).

| Check | What it tests |
|---|---|
| 1 — Unauthenticated 401 | `GET /api/gmb/connect` without a session returns `401` |
| 2 — Authenticated OAuth URL | `GET /api/gmb/connect` with a valid session returns `{ url }` pointing to `accounts.google.com/o/oauth2/v2/auth` with `access_type=offline`, `prompt=consent`, and `business.manage` scope |
| 3 — Callback no params | `GET /api/gmb/callback` with no query params redirects to `/settings/social?gmb=error` |
| 4 — Callback bad state | `GET /api/gmb/callback?code=x&state=invalid` redirects to `/settings/social?gmb=error` |
| 5 — Social page GMB row | `/settings/social` renders the "Google Business Profile" row |
| 6 — Connect button | Clicking Connect fetches `/api/gmb/connect` and navigates toward Google OAuth (navigation intercepted + aborted so the test stays on-app) |

**Key assertions:**

```
GET /api/gmb/connect (no session) → 401
GET /api/gmb/connect (session)    → 200 { url: 'https://accounts.google.com/...' }
OAuth URL contains access_type=offline, prompt=consent, business.manage scope
GET /api/gmb/callback (no params) → 307 → /settings/social?gmb=error
GET /api/gmb/callback (bad state) → 307 → /settings/social?gmb=error
/settings/social Google Business Profile row visible
Connect button triggers Google OAuth navigation
```

```bash
node scripts/app-15-gmb-connect.cjs
```

---

---

## Image Generation Unit Tests (`imagegen-*`)

### What unit tests are, and why they exist alongside functional tests

The functional tests (`app-*`, `onboard-*`, `e2e-*`) prove that the product works end-to-end — they click buttons, fill forms, and verify that images appear. They are the most intuitive tests to read because they mirror what a real user does.

**Unit tests** are narrower. They test a single function in complete isolation — no browser, no network, no database. The goal is to verify that a piece of logic produces the right answer given a specific input, without any of the surrounding infrastructure.

For PostGlider's image generation pipeline specifically, unit tests matter because the routing decisions (which model, which provider, which price tier) are pure logic — they are deterministic calculations over a small set of fields. A functional test can confirm that an image was generated, but it cannot efficiently check all the edge cases: what happens when a landscaping business gets combined with a face-identity request? What if the equipment brand name is already in the prompt? A unit test checks each of these scenarios in milliseconds, with zero API cost.

The `imagegen-*` scripts form a **test pyramid** — the base layer (01–05) tests logic, the top layer (07–08) tests the boundary between our code and an external API, and the functional tests above verify the full user journey.

<Callout kind="warning">
  **Fixed 2026-07-03 — the sync-guard had been silently broken.** `imagegen-02-policy` and
  `imagegen-05-fixtures` each read `lib/config/imageGenPolicy.ts` to assert their inlined rule
  copies match production. Both resolved the path as `path.join(__dirname, '..', 'lib', ...)` —
  but `scripts/` is a symlink into the shared `pg-projects` meta-repo (see `postglider-auto`'s
  `CLAUDE.md`), so `__dirname` resolves through the symlink and `'..'` lands in `pg-projects/`,
  not `postglider-auto/`. The sync-guard test had been throwing `ENOENT` and failing on every run
  — which meant nobody was actually comparing the inlined rules against production. They had
  drifted significantly: both files still tested a two-tier `UPGRADE` stage retired by
  `D-2026-06-20a` (the `GenStage` type no longer even allows constructing it), and
  `rule-workspace-remix`'s priority was stale (550 instead of the real 750). Both files rewritten
  to the correct path and current production rules, including new coverage for
  `rule-item-composite` (D-2026-07-03d).
</Callout>

---

### Running the imagegen suite

No dev server required. These scripts run anywhere Node.js is installed.

```bash
node scripts/run-all.cjs imagegen          # all seven scripts in order (~5 seconds total)

# Or individually:
node scripts/imagegen-01-sanitize.cjs
node scripts/imagegen-02-policy.cjs
node scripts/imagegen-03-cohort.cjs
node scripts/imagegen-04-normalize.cjs
node scripts/imagegen-05-fixtures.cjs
node scripts/imagegen-07-gemini-vertex-gen.cjs
```

| Script | What it tests |
|---|---|
| `imagegen-01-sanitize` | PII stripping (email, phone, credit card) and prompt truncation at 1800 characters |
| `imagegen-02-policy` | Routing rule priority, first-match-wins behavior, REJECT decisions, equipment-trade route, catch-all |
| `imagegen-03-cohort` | `cohortGate()` — retired to a flat `'pilot'` constant (D-2026-06-19b, the old percentage-ramp is gone); confirms it stays that way regardless of args |
| `imagegen-04-normalize` | `normalizeIntent()` signal-to-intent mapping: identity policy, equipment-trade derivation (DB-cached, `classification_codes` primary path, regex fallback), `needs_two_step_trade` enrichment skip, brand name injection |
| `imagegen-05-fixtures` | 15 end-to-end routing scenarios — each confirms the full `normalizeIntent → policyEngine` pipeline produces the expected routing decision |
| `imagegen-07-gemini-vertex-gen` | **Replaces `imagegen-07-composite`/`imagegen-08-remix-composite` (deleted 2026-08-08)** — both mirrored the raw fetch()/`x-goog-api-key` Gemini Developer API transport retired by the 2026-07-30 Vertex AI migration (D-2026-07-30f), silently testing dead code. Rewritten against the current `@google/genai`/Vertex SDK shape (`callGeminiImage()`), the real guardrail-prompt builders (verbatim from `compositePrompt.ts`), and all three composite exports' orchestration — `personInSceneComposite`, `sceneRemix`, `compositeImages` — including `urlToBase64()`'s content-type/min-size guards. Deliberately excludes `identityDefectDetector.ts`'s vision-judge defect-retry pipeline (a separate, larger, still-uncovered surface) — stubbed, not silently absorbed. |

---

### `fuzzer_tests` catalog traceability — `sync-fuzzer-test-markers.cjs`

`public.fuzzer_tests` (the toward-BDD Given/When/Then catalog, `.dwp/plans/bdd-test-management-system.md`)
gives every row a real sequential `id`, used as a human-facing label ("fz-006") per Ken's
2026-08-08 naming convention. The code implementing a row is marked directly in the script file:

```js
// #region fz-014 — Random navigation never lands on the error boundary
... the code implementing that row's when_action/then_assertion ...
// #endregion fz-014
```

Zero-padded to 3 digits, and recognized natively by VS Code/IntelliJ's `#region`/`#endregion`
code-folding — a real editor benefit, not just a grep target — chosen over line numbers because a
named region survives edits above it; a line number does not.

`sync-fuzzer-test-markers.cjs` scans every script file for these markers and writes the
**confirmed** location back into the row (`implementation_file`, `implementation_lines`,
`marker_verified_at`) — the marker in code is the source of truth, the DB fields are a verified,
re-checkable cache, not hand-typed prose that can drift out of sync with reality. Also flags two
real problems: a marker in code with no matching `fuzzer_tests` row (orphaned/typo'd), and a row
marked `'implemented'` with no marker found anywhere (a stale claim).

```bash
node scripts/sync-fuzzer-test-markers.cjs             # scan + write updates
node scripts/sync-fuzzer-test-markers.cjs --dry-run   # scan + report only, no writes
```

No browser, no cost — pure filesystem scan + a handful of Supabase REST calls. Run it after
adding, moving, or renumbering any `#region fz-<id>` marker, or after backfilling new
`fuzzer_tests` rows against already-written code.

---

### Layer 1 / Layer 2 test pattern

All `imagegen-*` scripts follow a two-layer structure.

**Layer 1 — pure logic (no external dependencies)**

The function under test is *inlined* into the script — meaning the logic is copied directly from the TypeScript source into the test file and run as plain JavaScript. No imports, no network calls, no database. These tests run in milliseconds and pass even with no `.env.local` or internet connection.

Why inline rather than import? The TypeScript source lives in `lib/` and edge functions; the test scripts are CommonJS (a different JavaScript module format). Inlining avoids a build step and keeps each script self-contained.

**Layer 2 — database / integration (live Supabase)**

These tests write to and read from the actual database via Supabase REST. They verify that the live schema matches what the logic assumes — column names, row existence, state round-trips.

Layer 2 tests always:
- Save the original database state at the start
- Restore it at the end (a `finally` block — code that runs whether the test passes or fails)
- Skip gracefully if `SERVICE_ROLE_KEY` is absent from `.env.local`

```bash
# To run only Layer 1 logic tests (skip the DB round-trips), e.g.:
NEXT_PUBLIC_SUPABASE_URL="" node scripts/imagegen-04-normalize.cjs
```

<Callout kind="info">
  The example above previously referenced `imagegen-06-circuit-breaker.cjs` — that script (and the
  `provider_circuit` table it tested) was retired 2026-07-01 when MuAPI image generation was
  removed (see the note further down this section). The current `imagegen-*` suite runs 01–05 and
  07–08; there is no 06.
</Callout>

---

### The sync-guard pattern

A key risk with inlined tests is **drift** — the production code changes but the test's inlined copy is not updated. The test keeps passing while the real code has a different behavior.

`imagegen-02` and `imagegen-05` solve this with a **sync-guard**: a test that runs first and reads `lib/config/imageGenPolicy.ts` as a text file, then asserts that every rule ID in the source file is present in the test's inlined copy, and vice versa. If anyone adds a new routing rule in production without updating the test, the sync-guard test fails immediately.

```
PASS  SYNC-GUARD: inlined rule IDs match lib/config/imageGenPolicy.ts
```

If this test fails, it means the inlined `POLICY_RULES` array in the test script is out of sync with the production source. Update it before continuing.

<Callout kind="warning">
  **Honesty note added 2026-08-07.** The sync-guard only checks that the *rule ID list* in
  `imagegen-02`/`imagegen-05` matches `lib/config/imageGenPolicy.ts` — it does not, and cannot,
  check that the *evaluator function's behavior* (`evaluatePolicy`/`ruleMatches`, hand-copied
  into both scripts per their own "mirrors lib/imagegen/policyEngine.ts" header comments) still
  matches production. All five of `imagegen-01`, `-03`, `-04`, `-07`, `-08` are the same pattern
  with **no sync-guard at all** — `normalizeIntent()`, `sanitizePrompt()`, `cohortGate()`,
  `personInSceneComposite()`, `sceneRemix()`, and `compositeImages()` are all hand-copied
  reimplementations of the real `lib/` source files, not imports. This is the exact anti-pattern
  this repo's own `CLAUDE.md` (Working hygiene item 9) documents as having already caused one real
  shipped bug (a hand-copied `confirmedPass()` reimplementation that inverted production's
  pass/fail bias in a defect-detection bench). A future change to any of those six real functions
  could silently diverge from what these unit tests check, and nothing here would catch it short
  of a human remembering to re-copy the logic by hand. If this repo wants durable protection here,
  the fix is the same one `CLAUDE.md` already prescribes elsewhere: export the real functions and
  import them into the test scripts instead of copying their bodies.
</Callout>

---

### imagegen-01-sanitize

Tests the PII (Personally Identifiable Information — emails, phone numbers, credit card numbers) stripping function that runs on every prompt before it is sent to any AI provider. Verifies:

- Email addresses are replaced with `[REDACTED]`
- Phone numbers in US formats are replaced with `[REDACTED]`
- 16-digit card numbers are replaced with `[REDACTED]`
- Prompts longer than 1,800 characters are truncated
- Normal prompts pass through unchanged

---

### imagegen-02-policy

Tests the policy engine — the function that looks at an image generation request and decides which routing rule applies. Rules are sorted by priority; the highest-priority matching rule wins.

The current production rules, in priority order:

| Rule ID | Priority | When it fires | Result |
|---|---|---|---|
| `rule-ssrf-block` | 1000 | URL failed allowlist validation | REJECT |
| `rule-upgrade-free-tier` | 900 | UPGRADE stage + free account | REJECT |
| `rule-face-preserve` | 800 | Face identity requested | → FACE_IDENTITY |
| `rule-upgrade` | 700 | Explicit quality upgrade | → UPGRADE |
| `rule-text-heavy` | 600 | Text must be legible in image | → TEXT_HEAVY |
| `rule-workspace-remix` | 550 | Source image provided, no face | → WORKSPACE_REMIX |
| `rule-person-safe` | 500 | Person in scene, no face reference | → PERSON_FOREGROUND |
| `rule-explore-trade` | 50 | Equipment-trade business | → EXPLORE_TRADE |
| `rule-default-general` | 0 | Everything else (catch-all) | → GENERAL |

A **catch-all rule** is one with no conditions (`when: []`) — it matches every request. It must always exist and have the lowest priority so it only fires when nothing more specific matched.

The test also verifies that `matched_rule_ids` records *all* matching rules, not just the winner. This powers the audit trail in `image_gen_provenance` — you can see why a generation routed a particular way even after the fact.

**Key new tests added (2026-06-05):**
- `is_equipment_trade=true → EXPLORE_TRADE` — the equipment-trade route was missing from this test entirely. A landscaping or HVAC business would have silently fallen through to the old catch-all.
- `is_equipment_trade=false → GENERAL` — confirms the false value (explicit DB override) is respected and does not trigger EXPLORE_TRADE.
- `is_equipment_trade=true + FACE_PRESERVE → FACE_IDENTITY wins` — confirms that a landscaper with a face reference still gets the identity composite route (priority 800 beats priority 50).
- Catch-all renamed from `rule-default-explore` (→ `EXPLORE`) to `rule-default-general` (→ `GENERAL`). The old name referred to a route key that no longer exists in production.

---

### imagegen-03-cohort

Tests the **canary gate** — the function that decides whether a given user should be in the `pilot` cohort (5% who see new features first) or the `control` cohort (everyone else). Verifies that:

- The same user ID always gets the same cohort (deterministic — *deterministic* means the same input always produces the same output, with no randomness)
- 5% of a large population falls in `pilot`
- 95% falls in `control`
- The percentage split is stable across repeated calls

---

### imagegen-04-normalize

Tests `normalizeIntent()` — the function that translates raw UI signals (what the user clicked, what their business profile says) into a structured `ImageGenIntent` object that the policy engine and routing layer consume.

**What it maps:**
- `userControls.use_profile_faces = true` → `identity_policy: 'FACE_PRESERVE'`
- `userControls.identity_strictness = 'STRICT'` → `identity_policy: 'PERSON_SAFE'`
- Storage URLs → object keys (strips the CDN prefix to get the storage path)
- Non-storage URLs → throws `SSRF_BLOCKED` (SSRF — Server-Side Request Forgery — is an attack where a server is tricked into fetching an attacker-controlled URL)

**New tests added (2026-06-05) — equipment-trade derivation:**

The `is_equipment_trade` field determines whether a business should use gpt-image-2 (brand knowledge, better for Toro mowers and DeWalt tools) versus Gemini 3.1 Flash Image (better scene quality for everything else). The derivation has two paths:

1. **DB-cached value** — `business_profiles.is_equipment_trade` was set when the profile was created or classified. This is preferred and trusted directly.
2. **Regex fallback** — if the DB value is `null` (unclassified profile, or a request built from a legacy code path), the function tests the `industry` text string against a pattern that matches landscaping, HVAC, plumbing, roofing, electrical, auto repair, and related trades.

Tests verify both paths and the edge cases:
- `is_equipment_trade: false` (explicit DB value) is respected even if the `industry` field looks like a trade — the DB is the authority
- `is_equipment_trade: null` + `industry: 'lawn care and landscaping'` → regex fires → `true`
- `is_equipment_trade: undefined` (no field at all) behaves the same as `null`

**New tests — `enrichTradePrompt` injection:**

When `is_equipment_trade` is true and the business profile has `equipment_names` (a list like `['Toro ProCore 648', 'Husqvarna Z246']` generated from the owner's equipment photos), the function appends the primary equipment name to the prompt. This activates gpt-image-2's brand recall — if the prompt says "featuring a Toro ProCore 648," the model renders that specific machine rather than a generic green mower.

- **No-op** (a term for "does nothing / returns without changing anything") cases verified:
  - Brand name already in the prompt → no duplicate injection
  - `equipment_names` is an empty list → prompt unchanged
  - `is_equipment_trade` is false → `enrichTradePrompt` is never called, even if names are present

---

### imagegen-05-fixtures

A **fixture** test is one where you define a set of inputs and expected outputs upfront, then run the full pipeline against each one and confirm the output matches. Think of it as a truth table for the routing engine.

This script runs 15 scenarios through the complete `normalizeIntent → policyEngine` pipeline and asserts the expected routing key for each:

| Fixture | Scenario | Expected route |
|---|---|---|
| f01 | Simple explore, no identity, no text | GENERAL |
| f02 | Text must appear in image | TEXT_HEAVY |
| f03 | Person in scene, no face reference | PERSON_FOREGROUND |
| f04 | Owner face reference provided | FACE_IDENTITY |
| f05 | Explicit quality upgrade, Pro account | UPGRADE |
| f06 | Quality upgrade, free account | REJECT |
| f07 | Face reference + text required | FACE_IDENTITY (higher priority wins) |
| f08 | `use_profile_faces` control set | FACE_IDENTITY |
| f09 | Quality upgrade, starter account | UPGRADE |
| f10 | Zero credit balance | GENERAL (credit gate is elsewhere) |
| f11 | Source image provided, no face | WORKSPACE_REMIX |
| f12 | Equipment-trade business (DB-cached) | EXPLORE_TRADE |
| f13 | Equipment-trade via industry text (regex fallback) | EXPLORE_TRADE |
| f14 | Equipment-trade + face reference | FACE_IDENTITY (priority 800 > 50) |

**Why f01 and f10 changed:** These fixtures previously expected `EXPLORE` (the old catch-all route key). The production catch-all was renamed to `GENERAL` when Gemini 3.1 Flash Image via MuAPI replaced the previous default provider. The test was asserting a route that no longer exists — it was passing against the stale inlined rules but would have produced the wrong model in production.

**Fixtures f12 and f13 are new.** Before 2026-06-05, the EXPLORE_TRADE route — responsible for routing landscapers, HVAC technicians, roofers, and plumbers to gpt-image-2 (which knows real equipment brand names) — had zero routing fixture coverage. If the `rule-explore-trade` rule was accidentally removed or its priority changed, no test would catch it.

---

### imagegen-07-composite

Tests `personInSceneComposite()` — the function that calls Google Gemini to produce an identity-preserving composite image (person's face from a headshot placed into a scene image). This is the core of the D016 architecture: Google Gemini direct at $0.067/image vs. MuAPI at $0.12.

**Why this test exists:** In June 2026, the D017 benchmark revealed that this function had two bugs that caused every identity composite via the Google provider path to silently fail since D016 deployed:
1. Invalid `generationConfig` fields in the request body caused a 400 error
2. A mismatch between camelCase (`inlineData`) and snake_case (`inline_data`) in the response parser meant the image data was never extracted even on successful responses

Neither bug produced a visible error message — the function simply never returned an image. There was no test to catch it.

**How the test works — HTTP mocking:**

A **mock** is a fake replacement for an external dependency. Instead of calling the real Google Gemini API (which would require an API key, cost money, and take several seconds), the test supplies a fake `fetch` function that returns pre-built responses. The function under test never knows the difference — it calls `fetch()` and receives a valid-looking response.

This means the tests run in milliseconds, cost nothing, and work with no API key or internet connection. They are testing that our code handles Gemini's responses correctly, not that Gemini itself works.

**What the tests cover:**

| Test | What it verifies |
|---|---|
| camelCase `inlineData` response | The normal Gemini API format is parsed correctly and returns `{ imageBase64, mimeType: 'image/png' }` |
| snake_case `inline_data` response | The fallback format (for API version changes) is also handled |
| Non-200 response | Error includes the HTTP status code and response body |
| Response with no image part | Throws a clear "Gemini: no image in response" error (rather than a cryptic `undefined` crash) |
| Image download failure | Throws with the URL and HTTP status when a scene/headshot image can't be fetched |
| Request hits correct endpoint | Asserts the Gemini endpoint URL and `x-goog-api-key` header are set correctly |
| Guardrail text ordering | The face-quality guardrail ("eyes fully open, preserve facial likeness") appears *before* the caller's scene description in the request — Gemini weights earlier text more heavily |
| All images included | Both scene and person images (plus any extras) are downloaded and included in the request |
| Extra images | When `extraImageUrls` is provided, all three images are in the request body |
| Non-JSON response | Throws a clear error when Gemini returns an HTML error page instead of JSON |

**The camelCase / snake_case distinction:**

In JavaScript/JSON, `camelCase` names each word with a capital letter after the first (`inlineData`, `mimeType`). `snake_case` uses underscores (`inline_data`, `mime_type`). Google's Gemini API uses camelCase, but an earlier version of the API used snake_case, and some error responses mix both. The production code handles both formats; the test verifies both paths are exercised.

---

### What these tests would have caught

Looking back at the commit history, the three gaps the new tests cover correspond directly to production bugs that required an investigation or benchmark to discover:

| Bug | How it was found | How long it was live | Test that would have caught it |
|---|---|---|---|
| EXPLORE_TRADE route not tested — any priority change would go unnoticed | Code review during audit | Months | `imagegen-02` sync-guard + `imagegen-05` f12/f13 |
| `normalizeIntent` didn't include `is_equipment_trade` in the returned intent | Code review during audit | Since EXPLORE_TRADE was added | `imagegen-04` equipment-trade tests |
| `personInSceneComposite` — invalid API fields + case mismatch → silent failure | D017 benchmark forced a real API call | Since D016 deployed | `imagegen-07` mock tests |

**Note:** `imagegen-06-circuit-breaker` was retired 2026-07-01 when MuAPI image generation was removed (D-2026-07-01b). The `provider_circuit` table it tested has been dropped.

The pattern is consistent: the bugs that survive longest are the ones that fail silently — no error message, no crash, just a wrong or missing result. Unit tests with explicit assertions are what surface these.

---

## Standalone image pipeline smoke tests (repo root)

These `.mjs` scripts live at the `postglider-auto` repo root (not in `scripts/`). They make real API calls and cost money — run them when validating a model change or a new pipeline stage, not on every deploy.

| Script | What it tests | Run command |
|---|---|---|
| `test-equipment-imagegen.mjs` | Full Trade route Step 1: keyword-matches 3 landscaper captions against `brand_assets.ai_tags`, generates an equipment rider via Gemini Flash, enriches the prompt, calls gpt-image-2 low, saves PNGs to `scripts/output/` | `node test-equipment-imagegen.mjs` |
| `test-gemini-finisher.mjs` | Step 2 identity composite in isolation: loads an existing scene PNG + a headshot URL, runs the expanded `identityInsert` guardrail (face swap + sign removal + misalignment fix), saves result to `scripts/output/`. Default model is `gemini-2.5-flash-image` ($0.067); `--pro` flag switches to `gemini-3-pro-image` (~$0.529/image — corrected 2026-07-16, token-billed, was wrongly documented as $0.134) | `node test-gemini-finisher.mjs` / `node test-gemini-finisher.mjs --pro` |

Output files land in `scripts/output/` (gitignored). Upload to `user-gallery-images` bucket for a shareable URL if needed.

---

## Known Gaps

This table is updated whenever a billing, auth, or subscription change ships without corresponding test coverage. It is the authoritative list of what the test suite does **not** verify.

| Gap | Affected script | What's missing | Introduced | Closed |
|---|---|---|---|---|
| `/sign-up` email capture form | `app-05-pricing-routing` | ~~Test asserts a redirect to `/pre-checkout`~~ | 2026-05-27 | 2026-05-27 (`app-05` Test 1 rewritten; full coverage in `app-09` Tests 1–3) |
| `POST /api/auth/pre-checkout` | — | ~~New route had zero test coverage~~ | 2026-05-27 | 2026-05-27 (`app-09` Test 4) |
| `/login?next=` redirect | `app-01-ux` | ~~No test covered `?next=` param behavior after login~~ | 2026-05-27 | 2026-05-27 (`app-09` Test 5) |
| WC customer mirror on claim | — | ~~No test verified the DB write or that both DBs had matching records after claim~~ | 2026-05-27 | 2026-05-27 (`app-09` Test 6 — verifies `woocommerce_customer_id` in profiles **and** matching email on WC customer record) |
| Variable SKU webhook DB write | `app-08-billing-coverage` | ~~Test 3 checks SKU display only (UI). No test sent a PG-PRO-3 payload through the webhook and asserted the DB write~~ | 2026-05-27 | 2026-05-27 (`app-08` Test 6) |
| Butler backlog E2E | — | ~~No test covered Butler scoring, ButlerReviewSheet open/close, approve (stage DB write), or skip (RPC + skip_until)~~ | 2026-06-02 | 2026-06-02 (`app-12-butler-e2e` Phases 1–8) |
| EXPLORE_TRADE route coverage | `imagegen-02`, `imagegen-05` | ~~Policy tests used stale inlined rules — catch-all pointed to non-existent `EXPLORE` route key instead of `GENERAL`; `rule-explore-trade` missing entirely from fixtures~~ | Since EXPLORE_TRADE added | 2026-06-05 (`imagegen-02` sync-guard; `imagegen-05` f12/f13/f14 + catch-all renamed) |
| `normalizeIntent` equipment-trade paths | `imagegen-04` | ~~`is_equipment_trade` derivation (DB-cached and regex fallback) and `enrichTradePrompt` injection not tested — the most domain-specific routing logic had no unit coverage~~ | Since equipment-trade classification added | 2026-06-05 (`imagegen-04` — 10 new tests covering all branches) |
| `personInSceneComposite` Gemini adapter | — | ~~Zero test coverage for the identity composite function; silent API field errors + camelCase/snake_case mismatch went undetected since D016 deployed~~ | Since D016 deployed | 2026-06-05 (`imagegen-07-composite` — 10 HTTP mock tests) |
| `e2e-generate` sequential isolation | `e2e-generate` | Chaining routes in one process causes stale idea/pack accumulation — test results degrade on 2nd+ route. Must run each route as a separate process. | 2026-06-11 | Documented — no code fix needed; architectural constraint |
| Autonomous pipeline E2E | — | No test covered the full new-user → autonomous batch → pack-verified journey. | 2026-06-18 | 2026-06-18 (`app-17-autonomous-e2e` — 7 phases: registration, profile verify, enable autonomous, trigger orchestrator, poll batch, verify DB, render pack page) |
| `e2e-generate` vault assertions | `e2e-generate` | ~~Vault tests waited for "Approve"/"Add to Pack" which never appear after single-photo vault generation (effectiveBatchSize=1 → GEN_COMPLETE → review state)~~ | Since vault route added | 2026-06-11 (`e2e-generate.cjs` — vault assertions rewritten to waitForResponse + button re-enable) |
| `e2e-generate` VaultPickerModal origin filter | `e2e-generate` | ~~`setupProfile()` keeperIds query included ai_generated images; VaultPickerModal only shows upload/gmb — setup kept invisible images, vault picker showed 0 tiles~~ | Since vault route added | 2026-06-11 (`e2e-generate.cjs` — added `origin=in.(upload,gmb)` to keeperIds query) |
| `imagegen-02`/`imagegen-05` sync-guard path | `imagegen-02`, `imagegen-05` | ~~Sync-guard resolved `lib/config/imageGenPolicy.ts` relative to the `scripts/` symlink's real target (`pg-projects/`) instead of `postglider-auto/` — threw ENOENT and had been silently failing every run, so inlined rules drifted undetected: a retired two-tier `UPGRADE` stage and a stale `rule-workspace-remix` priority (550, not 750)~~ | Unknown — since whenever the `scripts/` symlink was introduced | 2026-07-03 (both files: fixed path, removed retired `UPGRADE` fixtures/rules, added `rule-item-composite` coverage) |
| `sceneRemix`/`compositeImages` Gemini adapters | — | ~~Zero unit test coverage for either new function (D-2026-07-03c/d) — same silent-failure class `imagegen-07` already exists to catch for `personInSceneComposite`, but neither new function had a sibling test~~ | Since D-2026-07-03c/d shipped | 2026-07-03 (`imagegen-08-remix-composite` — 16 HTTP mock tests) |
| "AI Assisted" (`MULTI_IMAGE_COMPOSITE`) live generation | — | New 2-image composite feature (D-2026-07-03d) verified only at the UI-mechanics level (picker, button enablement) — no test has confirmed a real generated image lands correctly via this path | 2026-07-03 | Open |
| `WORKSPACE_REMIX` via `/generate` (Passenger, real account) | `app-20-new-composite-routes` | ~~D-2026-07-03c's Gemini migration was verified live only through the onboarding trial demo route — the main app's `/generate` path for a workspace-only business had not been exercised~~ | 2026-07-03 | Closed (`app-20` Phase 1 — real Passenger `/generate` run, routing_key + coherence asserted) |
| `app-18-calendar-day-zoom` has zero assertions | `app-18-calendar-day-zoom` | The script only `console.log`s/screenshots the calendar day-click result — it cannot fail regardless of what actually happens. Reads as a leftover manual-diagnostic script rather than a maintained regression test. | Unknown — found during 2026-08-07 catalog refresh | Open |
| `scripts/_shared/visualRegression.cjs` is orphaned | (none — no caller) | The pixel-diff/pixelmatch helper this doc previously credited to `app-04-mobile` is not imported by any script in the repo. `app-04-mobile` was rewritten into a plain smoke+overflow check at some point without this doc being updated to match, and no replacement visual-regression gate exists. | Unknown | Open — flagged, not fixed, during 2026-08-07 catalog refresh |
| Hand-copied `imagegen-*` unit-test logic has no sync-guard against evaluator drift | `imagegen-01`, `-03`, `-04`, `-07`, `-08` (and the evaluator body inside `-02`/`-05`) | Six real production functions (`normalizeIntent`, `sanitizePrompt`, `cohortGate`, `policyEngine`'s evaluator, `personInSceneComposite`, `sceneRemix`, `compositeImages`) are hand-copied into these test scripts rather than imported. The existing sync-guard only checks rule *IDs*, not evaluator *behavior*. See the callout in "The sync-guard pattern" above. | Since each script was written | Open |
| `app-08-billing-coverage` Test 5 accepts either outcome for admin | `app-08-billing-coverage` | The "Extra Posts" teal pill check logs a note but passes regardless of whether the pill is shown for the admin test account — a real regression here would not fail the suite. | Unknown | Open |
| Soft-check ("logs a warning, doesn't fail") patterns in live E2E scripts | `app-06-network-resilience` (duplicate-gen prevention), `app-12-butler-e2e` (null `butler_reasoning`), `app-17-autonomous-e2e` (GMB Offers Agent capture, Batch Rationale signal marks) | These specific checks would not turn a CI run red even if the underlying feature silently broke — found during the 2026-08-07 catalog refresh, not previously documented. | Unknown | Open |
| `live-checkout-dry-run.cjs` coupon expired | `live-checkout-dry-run.cjs` | The script's `kenpro3fittycint` coupon expired 2026-07-16; the script cannot complete a real checkout again without a fresh coupon. | 2026-07-16 | Open — needs a new coupon before this pattern can verify anything again |

---

## e2e-generate — Design Constraints and Pitfalls

The `e2e-generate` script is the most complex test in the suite. Each route/mode combination encodes assumptions about XState state transitions, UI element visibility, and database state that are not obvious from the code. These notes are the hard-won lessons from a full suite audit.

### Sequential isolation — one route per process

**Never chain e2e-generate runs in a loop.** The test account (`kenlyle+2@gmail.com`) accumulates ideas, packs, and PostForMe posts across runs. A second run that starts while stale ideas exist from the first run will pick them up in Architect scans, inflate `effectiveBatchSize`, and send the XState machine down a different branch than expected. Pack assembly finds too many ideas. TinderReview enters with 3 ideas when the test expects 1.

**Correct pattern:** Run each route independently as a separate process. Results are stable when run in isolation; they degrade when chained.

```bash
# Good — each route is a fresh process
node scripts/e2e-generate.cjs pilot trivial
node scripts/e2e-generate.cjs pilot story
node scripts/e2e-generate.cjs pilot vault

# Bad — stale state accumulates between routes
node scripts/e2e-generate.cjs pilot trivial && node scripts/e2e-generate.cjs pilot story
```

### Vault route — review state, not TinderReview

When a vault route runs with a single vault photo (the default), `effectiveBatchSize=1` causes the XState machine to send `GEN_COMPLETE` rather than `BATCH_COMPLETE`. The page returns to the settings panel — `GeneratorConfiguration` renders again — rather than entering TinderReview.

**Assertions that do NOT work after vault generation:**
- Waiting for "Approve"/"Reject" swipe buttons (only appear in `tinder.reviewing`)
- Waiting for "Add to Pack" (only visible when `isPilotMode=true` on an IdeaCard — not in review state)
- Waiting for "Schedule to Socials" (only visible when `saved=true`)

**Correct assertion pattern for vault:**
```javascript
// 1. Wait for the generate-text API to return 200
const responsePromise = page.waitForResponse(
    r => r.url().includes('generate-text') && r.status() === 200,
    { timeout: 120_000 }
);
await clickCreateMyBatch();
await responsePromise;

// 2. Wait for "Create My Batch" to re-enable (page returned to ready state)
await page.waitForFunction(
    () => {
        const btn = Array.from(document.querySelectorAll('button'))
            .find(b => b.textContent?.includes('Create My Batch'));
        return btn && !btn.disabled;
    },
    null,
    { timeout: 30_000, polling: 500 }
);
```

### Person In Scene nudge — suppress before clicking

Any test account with `headshot_1` set on its business profile will trigger the Person In Scene nudge modal when "Create My Batch" is clicked. The nudge intercepts the click and the generation never starts.

`clickCreateMyBatch()` in `e2e-generate.cjs` pre-sets `localStorage['pg_pis_nudge_dismissed'] = '1'` before each click. If you add a new click-path that bypasses `clickCreateMyBatch()`, this localStorage key must be set first.

### forcePassenger convention for story/vault routes

Story and vault routes use the personal-photo generate flow, which forces `is_pilot_mode_active=false` regardless of account state. `setupProfile()` detects `ROUTE === 'story' || ROUTE === 'vault'` and sets `forcePassenger=true` before writing the profile. Without this, the profile remains in pilot mode, the Assemble Plan flow activates, and the route never enters the single-photo path.

### VaultPickerModal origin filter — impacts setup

`VaultPickerModal.tsx` queries `user_images` with `.in('origin', ['upload', 'gmb'])`. Images with `origin='ai_generated'` are invisible to the picker regardless of how many exist in the DB.

`setupProfile()` in `e2e-generate.cjs` scopes its vault keeperIds query accordingly:

```javascript
// Keep only upload/gmb images — ai_generated images are invisible to VaultPickerModal
`/rest/v1/user_images?user_id=eq.${profile.user_id}&archived=eq.false&origin=in.(upload,gmb)&select=id&limit=${KEEP_FOR_VAULT}&order=created_at.desc`
```

If you update the keeperIds logic and forget this filter, cleanup will keep the N most-recent user_images (which may all be `ai_generated`), and the vault picker will show 0 tiles.

### VaultPickerModal — expand to all-time

The picker defaults to showing all-time images, but the script's `findUnselectedTile()` helper handles the case where all visible tiles are already selected. It expands to the full image set (via "Show all images" button) when no unselected tile is found in the initial view.

---

## checkGeneratePosts (shared step)

All three `onboard-*` scripts call `checkGeneratePosts` after a successful account claim, but only when onboarding produced a result and the claim form was completed. If onboarding was Turnstile-blocked (the most common headless outcome), there is no logged-in session and the step is skipped automatically.

**What it does:**

1. Navigates to `/generate`
2. Verifies the page loaded — fails gracefully if redirected to `/login`
3. Types a theme into the creative direction input if visible
4. Clicks the Generate button
5. Waits up to 60s for `Bank It / Approve / Reject` swipe buttons, `Caption` text, Turnstile block, or explicit error

**Pass/fail logic:**

| Outcome | Result |
|---|---|
| Idea cards visible (`Bank It`, `Approve`, `Reject`, `Caption`) | PASS |
| Turnstile blocked (expected in headless) | PASS |
| Generate button not found, page didn't load, or explicit error | FAIL |

Screenshots are written with the `17-generate-*` prefix in each script's output directory.

---

## StepCapture video guides

Real, interactive video walkthroughs of the app, captured live via `stepcapture-ultimate-batch.cjs` (see the Root-Level Scripts inventory above). Each is a real recording of the actual product, not a scripted screen mock — embed format confirmed live 2026-08-15 via StepCapture's own Share → Embed panel (`Video` tab).

<iframe src="https://app.stepcapture.com/capture/43b94fe1c135687c4c690b74512d11e8/embed?type=video" width="100%" height="400" title="Onboarding through candidate review — PostGlider" frameborder="0" referrerpolicy="strict-origin-when-cross-origin"></iframe>

**Onboarding through candidate review** (27 steps) — a full walkthrough from `/start` through GMB import, website mining, Owner Appearance %, and reviewing real Offer/Virality/Influencer/Retell candidates before batch generation.

More captures will be added here as they're recorded. The Tour Guide Agent (`app/api/autonomous/guide/route.ts`) can also surface a matching video inline in its in-app tour steps when one exists for the topic being asked about — see that route's `videoEmbedUrl` field (added 2026-08-15, decisions.md D-2026-08-15m).
